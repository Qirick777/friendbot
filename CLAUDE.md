# CLAUDE.md — AI 봇 프로젝트 행동 규약

> 이 파일은 Claude Code가 매 세션 시작 시 자동으로 읽는다. 짧게 유지한다.
> 상세 설계는 `AI_Bot_Design.md`에 있다. 이 파일은 "그것을 어떻게 쓸지"의 규칙이다.

## 프로젝트

마인크래프트 Forge 1.20.1 AI 동료 봇. `ServerPlayer`를 상속한 가짜 플레이어에 자체 AI를 얹는다.
전체 설계와 시공 순서(Phase 0~5, 태스크 T번호)는 프로젝트 루트의 **`AI_Bot_Design.md`**에 있다.

## 절대 규칙 (매 태스크에 적용)

1. **한 번에 한 태스크만.** 여러 태스크를 동시에 진행하지 않는다. 지정된 T번호 하나만 한다.

2. **매 태스크는 설계서 인용으로 시작한다.** 코딩 전에 `AI_Bot_Design.md`에서 해당 T번호 항목(목적/선행조건/분할근거/구현 스펙/검증)을 **그대로 인용**하라. 기억이나 추측으로 스펙을 채우지 마라. 문서에 없는 내용을 지어내면 그 결과물은 폐기 대상이다.

3. **선행조건을 먼저 확인한다.** 해당 태스크의 선행조건 T번호들이 이미 검증 PASS 상태인지 확인하라. 미완이면 멈추고 보고하라.

4. **스펙 100% 구현.** 인용한 구현 스펙의 모든 항목을 1:1로 코드에 매핑하는 체크리스트를 만들고, 하나도 빠뜨리지 마라. "위에 있으니 알아서" 식으로 뭉개지 마라.

5. **검증 명령어를 함께 만든다.** 기능 코드만 짜고 끝내지 마라. 그 태스크의 [검증] 항목을 구현하는 검증 명령어를 반드시 함께 작성한다.

6. **완료는 검증 로그로만 판정한다.** "코드를 짰다"는 완료가 아니다. 서버를 구동해 검증 명령어를 실행하고, `[BOTTEST] <T번호> PASS measured=<before→after>` 로그가 나와야 완료다. 로그를 반드시 출력하라.

7. **검증은 결과 변동으로만 판정한다.** "명령어가 호출됨 = OK"는 금지. 반드시 어떤 값이 실제로 변했는지(좌표 이동, 체력 감소, 상태값 변화 등)를 측정해 PASS/FAIL을 가른다.

8. **FAIL이면 그 자리에 머문다.** 검증 실패 시 다음 태스크로 넘어가지 마라. 원인을 로그·코드 근거로 찾고, 설계서 스펙과 대조해 수정하고, PASS 로그가 나올 때까지 그 태스크에 머문다.

## 검증 명령어 규약

- 커맨드 프레임워크(T0.2에서 구축)를 통해 등록한다.
- 구조: `setup()` 환경 조성(스폰/배치/지급) → 관찰 창(N틱) 폴링 → `judge()` 결과 변동 측정.
- 로그 형식: `[BOTTEST] <name> PASS/FAIL measured=<before→after> expected=<조건>`.
- 환경만 만들어 두고 대기한다. 원하는 상황이 나올 조건만 조성하고, 값 변동을 관찰한다.

## 빌드·실행

- JDK 17 고정 (21 사용 금지 — 빌드 실패).
- Forge 1.20.1 MDK (47.x), ForgeGradle, Parchment 매핑.
- `./gradlew genSources`로 디컴파일 바닐라 소스 확보 후, 실제 클래스 시그니처를 참조해 구현(추측 금지).
- 빌드: `./gradlew build`. dev 서버로 검증 실행.

## 진행 방식

각 태스크는 이 순서를 따른다:
```
설계서 인용 → 선행조건 확인 → 구현 계획(스펙 대조 체크리스트) → [승인 대기]
→ 기능+검증 코딩 → 서버 구동 → [BOTTEST] 로그 → PASS면 완료 보고 → [다음 지시 대기]
```
지정받지 않은 태스크를 먼저 시작하지 마라. 각 단계 후 지시를 기다린다.

## 하지 말 것

- 설계서를 안 읽고 "다음은 이거겠지" 추측으로 진행.
- 여러 태스크 한꺼번에 구현.
- 검증 없이 "완료" 선언.
- 검증을 "호출됨=성공"으로 얕게 판정.
- 스펙 항목을 "알아서 되겠지"로 생략.
- FAIL을 무시하고 다음으로 진행.
