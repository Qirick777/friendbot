# 마인크래프트 AI 동료 봇 — 설계서

> **플랫폼**: Forge 1.20.1 / **언어**: Java (JDK 17) / **대상**: 싱글플레이 협력 AI 봇
> **핵심 방침**: `ServerPlayer`를 상속한 "가짜 플레이어"에 자체 AI를 덮어씌운다.
> 플레이어의 외형·애니메이션·인벤토리·전투 판정을 상속으로 확보하고,
> 두뇌(판단)·이동(자체 A*)·시선만 직접 구현한다.

---

## 목차

- [1. 설계 철학](#1-설계-철학)
- [2. 아키텍처 개요](#2-아키텍처-개요)
- [3. 봇의 토대 (ServerPlayer 기반)](#3-봇의-토대-serverplayer-기반)
- [4. 이동 시스템](#4-이동-시스템)
- [5. 시선 시스템](#5-시선-시스템)
- [6. 전투 시스템](#6-전투-시스템)
- [7. 반사 계층 (즉시 반응)](#7-반사-계층-즉시-반응)
- [8. 생존·회복 로직](#8-생존회복-로직)
- [9. 유저 보호 프로토콜](#9-유저-보호-프로토콜)
- [10. 도주 모드 (납치 후 도주)](#10-도주-모드-납치-후-도주)
- [11. 환경 조작 (블록 설치)](#11-환경-조작-블록-설치)
- [12. 낙하 생존](#12-낙하-생존)
- [13. 유저 낙하 받기](#13-유저-낙하-받기)
- [14. 장비 관리자](#14-장비-관리자)
- [15. 생활 기능 (배고픔·수면)](#15-생활-기능-배고픔수면)
- [16. 기본 상태 (평상시 행동)](#16-기본-상태-평상시-행동)
- [17. 아이템·자원 조달](#17-아이템자원-조달)
- [18. 파라미터 총람](#18-파라미터-총람)
- [19. 미결·향후 과제](#19-미결향후-과제)
- [부록 A. 구현 상세](#부록-a-구현-상세)
- **[개발 로드맵 (시공 순서)](#개발-로드맵-시공-순서)** ← 하단, "어떻게 지을지"
  - [R.0 로드맵 사용 규칙](#r0-이-로드맵의-사용-규칙-반드시-준수)
  - [Phase 0 프로젝트 토대](#phase-0--프로젝트-토대) / [Phase 1 봇의 존재](#phase-1--봇의-존재) / [Phase 2 이동·시선](#phase-2--이동과-시선) / [Phase 3 전투 코어](#phase-3--전투-코어) / [Phase 4 생존·반사·보호](#phase-4--생존반사보호) / [Phase 5 생활·자원·통합](#phase-5--생활자원통합)

---

## 1. 설계 철학

### 1.1 왜 가짜 플레이어인가

목표는 **"완전히 자연스러운 플레이어처럼 보이고 행동하는"** 봇이다. 이 목표에서는 몹을 새로 만드는 것보다 `ServerPlayer` 상속이 압도적으로 저렴하다.

- **상속으로 공짜로 얻는 것**: 플레이어 스킨·모델·렌더링, 모든 아이템 사용 애니메이션(먹기·마시기·활·방패·투척), 방어구 착용 모습, 웅크림·수영 포즈, 인벤토리·배고픔·체력·경험치·포션 효과 시스템, 전투 판정(데미지·크리티컬·인챈트·쿨다운).
- **직접 만드는 것**: AI 두뇌(판단), 이동(자체 A*), 시선, 그리고 연결 없이 봇을 상주시키는 배관.

몹으로 갔다면 위의 "공짜" 목록을 전부 손으로 재현해야 하며, 특히 애니메이션 리깅이 프로젝트 최대 비용이 된다. 가짜 플레이어의 비용은 "연결 없는 상주 배관"이라는 **초기 장벽 하나에 집중**되며, 한 번 넘으면 이후 모든 행동이 상속으로 해결된다.

### 1.2 하드코딩을 피하는 두 가지 원칙

1. **전투: 측정 기반 일반 엔진.** 몹을 이름으로 분류(對워든, 對철골렘…)하지 않고, 런타임에 읽은 스탯(체력·공격력·속도·사거리)으로 전술을 그 자리에서 도출한다. 특수 케이스는 **워든과 크리퍼 정도만** 최소한의 데이터로 보완한다.
2. **이동: 능력 인식 A*.** 길찾기의 이웃 판정에 "봇이 지금 뭘 들고 있는가"(물통·보트·블록)를 넣어, 인벤토리에 따라 갈 수 있는 길이 동적으로 바뀐다.

### 1.3 자연스러움 최우선

모든 행동은 "완전히 자연스러운 마인크래프트 플레이어/몹처럼" 보여야 한다. 시선은 스냅이 아닌 부드러운 보간, 이동은 텔레포트가 아닌 물리 기반, 회피는 우아한 무빙으로 처리한다.

---

## 2. 아키텍처 개요

봇은 매 틱 다음 4계층을 순서대로 실행한다.

```
┌─────────────────────────────────────────────┐
│ 1. 인식 (Perception)                          │
│    사실 수집: 내 체력/인벤/배고픔,             │
│    타겟 목록과 각 스탯, 투사체, 유저 상태,     │
│    예상 피해량, 낙하 상태                      │
├─────────────────────────────────────────────┤
│ 2. 반사 (Reflex)  — 조건 즉시 실행, 판단 건너뜀 │
│    R0 토템 사전장착                            │
│    R1 방패/회피 (투사체·근접 공격 무빙 회피)    │
│    R2 낙하 생존                                │
├─────────────────────────────────────────────┤
│ 3. 판단 (Decision) — 모드 선택                 │
│    생존 우선순위 상태머신                       │
│    → 교전 = f(거리 × 적 스탯 × 승패 게이트)     │
├─────────────────────────────────────────────┤
│ 4. 실행 (Action)                              │
│    이동 입력 + 시선 + 아이템 사용              │
│    └ 회피 레이어 상시 겹침                     │
└─────────────────────────────────────────────┘
```

**판단과 실행의 분리**가 핵심이다. "무엇을 할지"(판단)와 "어떻게 할지"(실행)를 섞으면 상태 전환이 지저분해지고 모드가 매 틱 진동한다.

---

## 3. 봇의 토대 (ServerPlayer 기반)

### 3.1 상속 구조

```
LivingEntity
└── Player
    └── ServerPlayer
        └── AICompanionBot  ← 우리 봇
```

`ServerPlayer`를 상속하므로 형제 관계인 `Mob`의 AI(Goal·네비게이션)는 쓸 수 없다. 대신 이동·전투·시선을 전부 자체 구현한다(그래서 몹 AI 의존이 원천적으로 없다).

### 3.2 상주 배관 (초기 핵심 관문)

진짜 플레이어는 네트워크 연결(Connection)이 매 틱 `doTick()`을 호출해준다. 봇은 연결이 없으므로 다음 중 하나로 틱 루프에 태운다.

- **더미 커넥션 방식**: 가짜 `Connection`을 물려 서버의 플레이어 틱 루프에 편입.
- **틱 이벤트 방식**: 모드의 서버 틱 이벤트에서 봇의 `tick()`을 직접 호출.

클라이언트가 이 엔티티를 "플레이어"로 렌더링하도록 스폰 패킷·플레이어 정보(탭 리스트 엔트리)를 처리해야 한다. 싱글플레이(통합 서버)라 이 과정이 완화된다.

> **참고 레퍼런스**: Forge의 `FakePlayer`(일시적 컨텍스트용이라 그대로는 부적합, 출발점), Carpet Mod의 상주형 fake player 구조(연결 없이 살리는 배관 개념).

### 3.3 라이프사이클 (확정)

| 항목 | 결정 |
|---|---|
| **소환** | 스폰 에그(커스텀 아이템). 사용 시 봇 소환 + **에그 소비(소모형)**. |
| **중복 방지** | 봇은 **한 마리만**. 이미 존재하면 소환 거부(메시지 출력). |
| **상주·저장** | 월드에 저장. UUID로 봇 데이터(위치·인벤토리·장착 장비) 영속. **재접속·월드 리로드 후에도 복원**. |
| **사망** | 봇 인벤토리 전체를 죽은 자리에 드롭 → 봇 엔티티 제거 → "봇 존재" 상태 해제(재소환 가능). 스폰 에그는 이미 소비됐으므로 **새 에그를 구해야 재소환**(죽음에 무게). |
| **모델링** | Alex(마른 팔 버전) 플레이어 스킨·모델 사용. |

**복원 로직**: 월드 로드 시 저장된 봇 데이터가 있으면 봇을 다시 스폰·상주시킨다(3.2의 상주 배관 재실행). 봇 데이터가 없으면(미소환 or 사망 후) 스폰하지 않는다.

**존재 상태 관리**: 월드 저장 데이터에 "봇 존재 여부 + 봇 UUID"를 기록해, 소환 중복 방지와 복원의 단일 근거로 삼는다.

---

## 4. 이동 시스템

### 4.1 2계층 구조

이동은 성격이 다른 두 종류로 나뉜다. **둘 다 봇이 직접 제어**하며 그림자 몹 같은 외부 엔티티에 의존하지 않는다.

| 계층 | 용도 | 방식 | 지연 |
|---|---|---|---|
| **전술 이동** | 전투 정밀 기동(스트레이프·백스텝·사이드스텝·카이팅·크리 점프·투사체 회피) | 방향 벡터 → 이동 입력 직접 계산 | 0 |
| **전략 이동** | 배회·유저 추종·도주 목적지·먼 지점 접근 | 자체 A* → 노드 추종 → 이동 입력 | 경로 계산 비용 |

**원칙**: 길찾기가 필요한 이동만 A*에 맡기고, 즉각 반응이 필요한 전투 기동은 봇이 직접 입력한다. 전투 반응성과 이동 견고함을 둘 다 잡는다.

### 4.2 공통 실행 방식

모든 이동은 결국 **입력값만 세팅하고 바닐라 `travel()` 물리가 처리**한다. 좌표를 직접 밀지(텔레포트) 않는다.

```
매 틱 이동 실행:
  - 몸통 yaw = 이동 방향
  - 전진/좌우 입력 (zza, xxa)
  - 점프 입력 (앞 블록이 한 칸 높으면)
  - 웅크림 입력 (setShiftKeyDown)
  → travel()이 중력·충돌·계단 오르기·마찰 자동 처리
```

이 방식의 이점: 웅크림 시 낭떠러지 안 밟기 같은 바닐라 로직이 자동 적용된다.

### 4.3 자체 A* (전략 이동)

**플레이어 히트박스·능력을 기준으로** 격자 위에서 표준 A*를 수행한다. 몹 기준이 아니므로 히트박스 불일치가 없다.

- **격자/노드**: 노드 = 블록 좌표. 이웃 = 인접 블록(수평 + 오르내림).
- **이웃 유효성 = 플레이어 이동 규칙**:
  - 목표 칸에 플레이어 히트박스(폭 0.6, 높이 1.8)가 들어갈 공간이 있는가.
  - 발밑이 받쳐지는가.
  - 1블록 오르기는 가능, 2블록 이상은 불가.
  - 낙하 데미지 안 나는 높이(기본 3블록)면 통과, 초과는 위험 비용.
  - 위험 칸(용암·선인장·낭떠러지)은 금지 또는 고비용.
- **비용 함수**: 기본 이동 + 위험 가중치 + 선호(평지 저비용). 블록 가중치를 직접 정의.
- **성능 관리**: 탐색 노드 상한(예산), 여러 틱 분할 계산, 경로 캐싱, 목표가 크게 안 바뀌면 재계산 스킵. (무한 탐색 렉 방지 — FPP의 give-up budget과 같은 안전장치.)

### 4.4 능력 인식 A* (인벤토리 연동)

경로 요청 시 **능력 스냅샷**을 A*에 넘긴다. 인벤토리에 따라 갈 수 있는 길이 달라진다.

```
능력 스냅샷 = {
  물통 보유?, 보트 보유?,
  설치용 흔한 블록 개수, 최대 안전 낙하(기본 3), ...
}
```

**하강 규칙**:

| 낙하 칸수 N | 조건 | 결과 |
|---|---|---|
| N ≤ 3 | — | 통과, 기본 비용 |
| N ≤ 물통한계 | 물통 보유 | 통과 + MLG 물낙법 비용(페널티), 노드에 "물 사용" 액션 태그 |
| 대형 | 보트 보유 | 통과 + 보트 낙법 비용, 노드에 "보트 탑승" 액션 태그 |
| 그 외 | — | 금지(낙사) |

**상승 규칙 (파일러 업)**:

```
위로 오르려는데 막힘(2칸 이상 벽):
  설치용 블록 보유 → "발밑에 블록 쌓고 오르기" 이웃 추가
    비용 = 블록당 설치 시간(느리니 페널티)
    노드에 "블록 설치" 액션 태그
  블록 없음 → 우회로 탐색, 없으면 도달 불가
```

**블록 선택 규칙 (고가치 보호)**:
- **제외**: 고가치·비재생·기능성 블록(다이아/네더라이트 블록, 광석, 셜커상자, 작업대·화로 등).
- **선호**: 흔하고 값싼 블록(흙·돌·조약돌·자갈·네더랙·판자). 우선순위: 개수 많은 것 > 재획득 쉬운 것 > 값싼 것.
- 판정: 스택 크기·희소성 정량 + 소수 블랙리스트 태그. (장비 관리자의 가치 판정 재활용.)

**특수 이동 비용 원칙**: MLG·보트·파일러업은 "가능하지만 비싸게" 매긴다. 실행 실패 리스크·소요 시간을 반영해, 안전한 걸음길이 있으면 우선하고 지름길 이득이 페널티보다 클 때만 쓴다.

**액션 태그 → 실행 연결**: A*는 경로에 표식만 남기고 실제 실행은 기존 모듈이 담당한다.

| 액션 태그 | 실행 담당 |
|---|---|
| "물 낙법" / "보트 낙법" | R2 낙하 생존 |
| "블록 설치" | 구조물 배치기 |
| "고가치 제외" | 장비 관리자 가치 판정 |

---

## 5. 시선 시스템

시선은 두 값으로 분리해 자연스러움을 만든다.

| 값 | 따라가는 대상 |
|---|---|
| **몸통 yaw** (`yBodyRot`) | 이동 방향 |
| **머리 yaw/pitch** (`yHeadRot`, `xRot`) | 주시 대상(전투 중=타겟, 평상시=이동 방향) |

몸통과 머리가 독립적으로 움직여 "걸으면서 적을 쳐다보는" 자연스러움이 나온다.

**각속도 상한 보간**: 목표 각도로 즉시 스냅하지 않고 "틱당 최대 N도"씩 접근한다. `LookControl`도 자체 구현으로 통일(몹 의존 제거). 로직은 짧다: 현재각 → 목표각 차이를 최대 회전량으로 클램프.

---

## 6. 전투 시스템

### 6.1 측정 기반 일반 엔진 (2층 구조)

```
┌────────────────────────────────────────────┐
│ 층 1: 일반 전투 엔진 (측정 → 규칙 → 전술)    │ ← 모든 몹(모드 몹 포함)
├────────────────────────────────────────────┤
│ 층 2: 몹 지식 사전 (얇은 데이터)             │ ← 워든·크리퍼만
└────────────────────────────────────────────┘
```

**규율: 층 2는 "코드"가 아니라 "데이터"다.** 전술 로직은 층 1에만 있고, 층 2는 규칙에 먹일 값 몇 개일 뿐이다. 층 2가 비어도 층 1이 홀로 잘 싸운다.

### 6.2 측정값

| 측정값 | 출처 | 의미 |
|---|---|---|
| 최대 체력 | health attribute | 탱킹력 |
| 공격력 | attack damage attribute | 한 대의 위력 |
| 이동 속도 | movement speed attribute | 카이팅 가능성 |
| 넉백 저항 | knockback resistance attribute | 밀어내기 통하나 |
| 원거리 여부 | 투사체 발사 관측 | 붙기 전에 맞나 |
| 방어 관통 | 특수공격 플래그(층 2) | 방패·갑옷 소용 |
| 히트박스 | 크기 | 근접권 밖 거리 |

> **[보완 · T4.6 사후 감사 승인] 규칙1의 속도 입력 = 관측 실측 유효 접근속도**
>
> (a) 위 표의 「이동 속도 | movement speed attribute」 행은 그대로 둔다. 다만 **규칙1(카이팅 가능성)이
> 소비하는 값**은 어트리뷰트가 아니라 **관측 실측 유효 접근속도**로 한다:
> `유효 접근속도 = 관측 창 내 대상 변위 ÷ 경과 틱` — **정지 틱을 분모에 포함**한다(최고속도도,
> 이동 틱만의 평균도 쓰지 않는다). 카이팅이 묻는 것은 순간 최고속이 아니라 시간당 실제 접근률이기
> 때문이다. 어트리뷰트는 **관측 표본이 쌓이기 전 콜드스타트 사전값**으로만 남으며, 그 사전값은
> **보수적으로 카이팅 불가(정면 대응)**로 한다. 빠른 몹을 느리다고 오판하면 봇이 등을 보이고 도망치다
> 잡히므로, 반대 오판보다 훨씬 비싸다. 판정 경계에서 떨지 않도록 진입·해제 임계를 분리한다(히스테리시스).
>
> (b) 근거(실측): 어트리뷰트 대비 실측 비율이 **0.496(좀비)~1.688(속도 0.8 좀비)로 3.4배 산포**하고
> 어트리뷰트에 따라 단조 증가한다. 실효속도 = MOVEMENT_SPEED × AI 태스크 속도배수이고 그 배수가
> 몹·AI 상태마다 다르기 때문이다(워든 0.933이 이상치 증거). 역방향 환산도 불가하다: 스프린트 플레이어
> 어트리뷰트 0.13 < 좀비 0.23인데 실측은 0.2806 > 0.1142로 반대다. **플레이어와 몹의 어트리뷰트는
> 단위가 다르므로 어느 방향으로도 환산 비교가 성립하지 않는다.** 따라서 단일 환산 계수는 폐기하고
> 새 계수를 만들지 않는다(콜드스타트가 보수 기본값이므로 계수가 필요 없다).
>
> (c) 이 패턴은 6.6의 「사거리 모름 → 관측된 첫 공격 거리로 안전거리 갱신(맞아보며 학습)」과 동일 계열이다.
> 즉 "보수적으로 가정하되, 관측되면 갱신한다".

### 6.3 전술 도출 규칙 (몹 이름 불문)

**규칙 1 — 카이팅 가능성**
```
적 속도 < 봇 스프린트 속도 → 카이팅 성립(도망치며 활 딜)
적 속도 ≥ 봇 속도        → 카이팅 불가(정면 대응)
```

**규칙 2 — 승패 게이트 (근접 진입 가부)**
```
봇이 적 죽이는 시간 = 적 유효체력 / 봇 DPS
적이 봇 죽이는 시간 = 봇 유효체력 / 적 DPS(관통 반영)

전자 < 후자 × 안전계수 → 근접 허용
아니면               → 원거리 강제
```

**규칙 3 — 안전 거리**
```
안전거리 = max(적 근접 도달거리, 적 원거리 사거리) + 여유
→ 이 값이 곧 카이팅 밴드가 된다
```

**규칙 4 — 방패·넉백 유효성**
```
적이 방어 관통 → 방패 전술 끔
적이 넉백 면역 → 밀어내기 대신 거리 벌림
```

이 네 규칙의 조합으로 전술이 자동 생성된다. **철골렘·워든·모드 보스가 모두 같은 규칙으로 처리**된다.

### 6.4 정밀 엔진

**엔진 A — 투사체 예측 조준 (활·엔더펄 공용)**
활과 엔더펄은 같은 탄도 문제(중력·항력 포물선)다. 반복 수렴(iterative intercept)으로 조준한다.
```
1. 최근 몇 틱 위치 변화로 타겟 속도 추정
2. 현재 위치까지 투사체 비행 시간 t 계산
3. 예측 위치 = 현재 + 속도 × t
4. 예측 위치 기준 t 재계산 → 2~3회 반복 수렴
5. 발사각(pitch) 역산 (화살 중력 ≈0.05/틱, 투척물 ≈0.03/틱, 항력 ≈0.99/틱 — 실측 확인)
6. 활은 풀차지(약 20틱) 후 발사(급하지 않으면)
```
봇은 각도를 정확히 넣을 수 있어 고명중이 자연히 나온다. 엔더펄 착지점 선택은 조준의 역방향(이탈=안전지점, 복귀=근접지점).

**엔진 B — 크리티컬 근접 타이밍**
바닐라 크리 조건: 낙하 중 + 지면 아님 + 사다리/물 아님 + **스프린트 아님** → 1.5배. 공격 쿨다운 충전율 ≈1.0 필요.
```
1. 스프린트로 사거리까지 접근
2. 사거리 진입 + 쿨다운 충전 완료:
     스프린트 해제 → 짧게 점프 → 하강 프레임에서 공격(크리 발동)
3. 타격 직후 다시 스프린트 접근, 쿨다운 재충전, 반복
4. 타격 사이 타겟 주위 원형 스트레이프
```
쿨다운 무시 연타는 데미지 반토막이므로 충전율 게이트가 필수.

### 6.5 특수 케이스 (층 2 — 워든·크리퍼만)

**워든** — 측정 규칙으로 자동 도출되지만, 소닉붐 관통성·정확 사거리·차징 신호는 측정 불가라 데이터로 보완.
- 스탯: 체력 500, 근접 최대 45(하드), 소닉붐 고정 10(방어구·방패·블록 관통, 저항 포션으로만 경감).
- 소닉붐 발동 조건: 타겟 감지 후 10초 + 마지막 공격 후 5초 + 수평 15블록·수직 20블록 이내. 차징 1.7초(제자리 정지) + 쿨다운 1.3초.
- 워든은 스프린트 플레이어보다 느림 → 규칙 1이 자동으로 "카이팅 가능" 판정.
- **도출 전술**: 16~20블록 밴드 유지 + 고명중 활 카이팅 + 방패 오프 + 공간 없으면 엔더펄 재배치. 소닉 차징 감지 시 즉시 밴드 밖 이탈(반사).
- Darkness 효과는 봇에 무영향(좌표로 추적).

**크리퍼** — 폭발 점화 거리·타이밍이 측정 불가.
- 점화(fuse) 상태 감지(부풀기) → 점화거리(약 3블록) 밖 유지가 절대 규칙.
- 활로 처리하거나, 근접 시 치고 빠지되 폭발 카운트다운 감지하면 즉시 이탈. 붙어서 안 싸움.
- 유저 보호: 크리퍼 벽 프로토콜(11장).

### 6.6 미지 몹 안전 기본값 (적응)

- 특수 능력 모름 → 차징/예비동작 감지 반사는 항상 켬(관측 기반, 몹 무관).
- 관통 여부 모름 → 방패 유효 가정하되, 방패가 뚫리는 걸 관측하면 즉시 방패 오프.
- 사거리 모름 → 관측된 첫 공격 거리로 안전거리 갱신(맞아보며 학습).

---

## 7. 반사 계층 (즉시 반응)

판단보다 먼저, 매 틱 최우선으로 검사한다. 우선순위 순.

### R0 — 토템 사전 장착

**사후가 아니라 선제적**으로 다룬다.
```
매 틱 예상 피해량 추정(근접 예상 데미지 + 투사체 예상 + 환경 피해)
IF 현재 체력 ≤ 예상 최대 피해 + 여유:
   → 오프핸드에 토템 즉시 장착하고 유지
```
토템은 손에 들려만 있으면 치명타 시 바닐라가 자동 발동한다. R0의 임무는 "위험할 때 오프핸드를 비워 토템으로 채우기"뿐. 발동 직후 이탈 모드로 전환해 재정비.

### R1 — 방패/회피

```
적 공격 모션 or 투사체가 봇 히트박스로 향함:
  방패 보유 → 방패 즉시 올림(오프핸드 use)
  아니면    → 수직 방향 사이드스텝(궤적 법선으로 1~2틱)
```

**방패 공방 사이클** (근접 모드 통합):
```
적 공격 감지 → 방패 올림
적 타격 직후(= 공격 쿨다운 창) → 방패 내리고 크리 1~2방 → 다시 올림
도끼류 적(AXE_WIELDER) → 방패 디스에이블(5초 잠김) 주의, 방패 의존 낮추고 회피 위주
카이팅 중 → 방패 내림(이동 느려짐 방지)
```

**투사체 회피 (모든 원거리 공격 무빙 회피)**: 블레이즈·스켈레톤·쇠뇌 피글린/피민·위더 등의 투사체는 무빙으로 우아하게 피한다. 봇을 향해 날아오는 투사체를 인식 계층에서 잡아 사이드스텝.

**오프핸드 우선순위 충돌 규칙**: **R0 토템 > R1 방패**. 치사 위험 시 방패를 내리고 토템이 오프핸드를 차지. 안전해지면 방패 복구.

### R2 — 낙하 생존

12장 참조.

---

## 8. 생존·회복 로직

### 8.1 생존 우선순위 상태머신 (판단 계층)

위에서부터 조건 만족 시 그 모드로 확정(위가 이긴다). 체력은 봇 자신 기준.

```
우선순위 0 — 생존 이탈
  봇 체력 < 위험선 AND 이탈 수단 보유:
    엔더펄 보유 → PEARL_ESCAPE
    없음        → RETREAT_HEAL(뒷걸음 + 회복)
  ※ 엔더펄은 5데미지(2.5칸) 주므로 현재체력 > 펄데미지 + 여유 검사 필수

우선순위 1 — 회복 중 유지
  이미 이탈해 회복 중 AND 체력 < 복귀선 → 회복 계속

우선순위 2 — 펄 복귀
  체력 ≥ 복귀선 AND 직전 펄 이탈 상태 → PEARL_RETURN

우선순위 3 — 전투 중 회복 아이템
  체력 < 경계선 AND 회복 아이템 보유 → 사용
    스플래시 치유 포션 = 즉발, 이동 제약 없음 → 발밑 투척, 전투 유지 (최우선)
    황금사과/마시는 포션 = 소비 중 이동 느림 → 거리 벌린 뒤 사용

우선순위 4 — 근접 교전
  타겟 근접 사거리 안 + 검 보유 → ENGAGE_MELEE(크리)

우선순위 5 — 원거리 교전
  멀거나 활+화살 보유 → ENGAGE_RANGED / 강적이면 KITE

우선순위 6 — 접근/재배치
  그 외 → 타겟 접근 or 유리한 위치 이동
```

### 8.2 봇 위급 기준

봇 체력 **40% 이하 = 위급**. 이 상태에서 유저 방어와 자기 회복이 충돌하면 → **유저 방어 우선(위험 감수)** (9장 참조).

---

## 9. 유저 보호 프로토콜

### 9.1 전투 개입 조건 (언제 싸우나)

봇은 **불필요한 선제공격을 하지 않는다.** 다음 중 하나면 개입:
- 유저가 공격받으면 자동 방어.
- 유저에게 어그로가 끌린 적을 선제 타격.
- 유저가 전투를 개시하면 동참.

### 9.2 타겟 우선순위

기본은 **가장 위험한(고DPS) 적 우선**. 단 유저 상태에 따라 전환:

```
유저 체력 > 40%:
   → 가장 위험한 적 우선 타격

유저 체력 ≤ 40% AND 봇 위급 아님:
   → 유저를 노리는 적부터 처치

유저 체력 ≤ 40% AND 봇도 위급(≤40%):
   → 고위험 적부터 (유저 방어 우선, 봇 위험 감수)
   → 고위험 적 여럿이면: 유저를 노리는 고위험 적부터

유저 체력 ≤ 15%(치명선) AND 회복 물약 보유:
   → 유저에게 즉각 회복(발밑에 투척 회복 포션)
   → 회복 후에도 여전히 치명선 이하 → 즉각 도주 모드(폴백)

유저 체력 ≤ 15%(치명선):
   → 즉각 도주 모드(납치 후 런)
```

### 9.3 원거리 몹 대응 (유저 타격 시)

**활 보유 시**: 유저 곁을 지키며 활로 원거리 처치. 유저에게 접근 중인 근접 위협이 있으면 그것부터 처치.

**활 미보유 시**:
1. 유저에게 어그로 끌린 근접 몹이 없으면 → 즉각 추적해 근접 처치.
2. 추적 중 유저와 너무 멀어지면 → 무시하고 유저에게 빠르게 복귀. (단, 무작정 뛰지 않고 자신을 향한 투사체는 우아하게 무빙 회피.)
3. 애초에 유저에게 어그로가 안 끌린 원거리 몹은 무시.

### 9.4 근접 몹 침범 판정

근접 몹은 이동 속도를 고려해 **유저 중심 10블록** 안에 들어오면 타격 대상으로 삼는다.

---

## 10. 도주 모드 (납치 후 도주)

### 10.1 발동 (예고·전의추론 없음, 즉각)

```
유저 체력 ≤ 15%(치명선):
  1순위: 투척 회복포션(스플래시/링거링 치유) 보유
         → 유저 발밑에 즉시 투척
         → 치명선 위로 회복되면 납치 취소, 전투 지속
  2순위: 회복으로 못 살림(포션 없음 or 회복 후에도 치명선)
         → 즉각 startRiding 후 질주
```

### 10.2 탑승 메커니즘 (싱글플레이)

강제 탑승의 클라이언트 디싱크는 **멀티플레이 문제**다. 싱글플레이(통합 서버)에서는 서버 발동 탑승으로 충분하다.

```
서버에서 player.startRiding(bot) 실행
→ SetPassengers 패킷이 클라이언트에 즉시 통지(같은 프로세스)
→ 클라이언트가 탑승 수락(플레이어가 클릭 안 해도)
```
"플래그만 세팅"이 아니라 **"탑승 패킷으로 통지"**가 핵심. (우클릭 흉내 같은 꼼수는 불필요하며 입력 장치 제어 불가로 구현도 안 됨.)

### 10.3 시퀀스

```
1. 봇이 유저에게 경로 계산해 달려옴
2. 인접 시 startRiding 실행 → SetPassengers 통지
3. 봇: passenger 오프셋을 머리 위로 반환 + 두 팔 든 포즈 렌더
4. 안전 방향 전력 질주(엔더펄 병행)
5. 안전권 도달 시 stopRiding
```

### 10.4 탑승 중 유저 입력

**시점(마우스) 자유, 이동은 봇 담당.** 유저는 머리 위에서 상황을 계속 본다. (팔 든 포즈는 봇 자신의 렌더 커스터마이즈라 문제없음.)

### 10.5 미확인 (실측 필요)

강제 탑승 시 플레이어가 격렬히 움직이거나 점프 입력 시 미세 떨림 가능성. 실제 테스트로 확인.

---

## 11. 환경 조작 (블록 설치)

`ServerPlayer` 기반이라 블록 놓기(`useItemOn` 계열)도 상속. 제작은 유저 의존이지만, **블록 배치는 전투 생존 핵심이라 봇이 직접** 한다.

### 11.1 구조물 배치기 (반사 계층에서 호출)

**크리퍼 벽 프로토콜**:
```
크리퍼 점화 감지 AND 폭심 근처에 유저 or 봇:
  A. 블록 보유 + fuse 시간 충분:
       크리퍼와 (유저/봇) 사이 직선상에 블록 설치
       → 폭발 레이캐스트가 고체 블록에 막혀 데미지 급감
  B. 자기 보호 동시:
       방패 보유 → 폭발 방향으로 방패
       블록만 → 자기 앞에도 쌓기
  C. 이탈 병행:
       설치와 동시에 점화거리 밖으로 스텝
  ※ fuse가 거의 끝났으면 설치 대신 방패+이탈(설치 시간 부족)
```

**기타 용도**:
- 화살 엄폐: 원거리 적의 화살 라인을 블록으로 차단.
- 긴급 격벽: 다수 근접 적 포위 시 유저 주위 임시 벽.
- 다리 놓기(브리징): 낭떠러지 앞 (우선순위 낮음, 향후).

### 11.2 설치 판정

- 설치 위치: 두 점 잇는 선상에서 대상에 가까운 빈 칸 + 아래가 받쳐지는 칸.
- 설치 가능성: 칸이 비었고 인접 면에 놓을 수 있는지(공중 불가).
- 블록 재고: 장비 관리자에 "방어용 블록 재고" 추적. 없으면 방패·이탈로 폴백.
- 블록 선택: 고가치 제외(4.4의 블록 선택 규칙 공유).

---

## 12. 낙하 생존 (R2)

계산 없이 두 트리거로 처리한다.

```
매 틱:
  낙하중 = (지면 아님) AND (하강 중) AND (fallDistance > 안전값)
  IF 낙하중:
     d = 발밑 아래로 레이캐스트한 지면까지 거리
     IF d ≤ 사거리:  ← "지금 뿌리면 닿는다"
        발동
```

`fallDistance`는 바닐라가 이미 추적하므로 새 계산 불필요(상속 이점).

**우선순위**:
```
낙하중 AND 아래거리 ≤ 사거리:
  IF 물통 보유:                        → 아래에 물 (데미지 가능성만 있어도, 최우선)
  ELIF fallDistance 치명적:
       IF 슬라임/짚/침대 보유:         → 아래에 설치
       ELIF 보트 보유:                 → 아래 놓고 탑승
       ELSE:                           → 감수
```

- **물통**: 낙하 데미지 가능성만 있으면 즉시(치명 여부 불문). 데미지 완전 무효 + 회수 가능이라 헤프게 사용. 물 사거리는 짧게(약 4블록).
- **슬라임/짚/침대/보트**: 소모·번거로움 있으니 치명적 낙하에서만. 물 없을 때 폴백.

---

## 13. 유저 낙하 받기

봇이 유저의 낙하를 받아 데미지를 없애는 연출(능동적, "b안").

### 13.1 성립 조건

```
유저_낙하중(유저 fallDistance > 안전값)
AND 봇이 유저 하강 경로 아래(수평거리 가깝고 + 봇 y < 유저 y)
AND 봇이 지면/안정 상태
→ 받기 발동
```

### 13.2 우선순위 (폴백 사슬)

```
유저 낙하 중:
  IF 봇이 이미 낙하선 아래(받을 수 있음):
       → 탑승 받기(데미지 0 + 연출, 가장 우아)
  ELIF 봇이 근처 + 착지지점 갈 시간 됨:
       → 마중 나가 받기, 실패 시 아래에 물/블록
  ELIF 유저에게 물/블록 깔아줄 수 있음:
       → 착지 지점에 물/블록
  ELSE:
       → 개입 불가(거리 부족)
```

### 13.3 재활용 부품

| 필요 | 출처 |
|---|---|
| 유저 낙하 감지 | R2의 fallDistance 판정을 유저에 적용 |
| 탑승시키기 | 도주 모드의 startRiding + 머리 위 오프셋 |
| 착지 지점 레이캐스트 | R2의 아래 거리 재기 |
| 물/블록 폴백 | 구조물 배치기 |

신규 요소는 "봇이 유저 낙하선 아래인가" 판정(좌표 비교)뿐. 받은 후 지면이면 즉시 stopRiding(도주와 달리 질주 안 함).

---

## 14. 장비 관리자

전투 로직과 분리된 **상시 백그라운드**. 인벤토리 변화 시에만 재평가.

**갑옷**: 4슬롯 각각 점수화 후 최고점 착용.
```
점수 = 방어도 + 방어 강도 + 인챈트 가치(보호/가시/내구) − 내구 위험
```
재질 티어는 방어도 attribute에 반영되므로 attribute 직접 비교(이름 불문).

**무기**: 근접·원거리 각각 최고 선정 후 상황에 맞게 스왑.
```
근접 점수 = 공격력 + 공격 속도 + 인챈트(예리함/강타/휩쓸기)
           (대상이 언데드면 강타 가중치↑)
원거리    = 활 인챈트(힘/무한/펀치) 최고 + 화살 재고 추적
```
교전 모드가 무기를 요청하면(근접→검, 원거리→활) 해당 카테고리 최고를 메인핸드로 스왑.

**방어용 블록 재고**: 구조물 배치기용 흔한 블록 추적(4.4 블록 선택 규칙과 공유).

---

## 15. 생활 기능 (배고픔·수면)

### 15.1 배고픔 (백그라운드 자동)

```
배고픔 수치가 임계 이하 → 인벤토리 최적 식량 섭취
  식량 선택: 포만감·포화도 높은 것 우선, 위험 식량(썩은 고기·복어·독감자) 회피
  전투 중 억제: 먹는 중 이동 느려짐 → 위급 회복(황금사과) 아니면 안전할 때
```
포만도 6 이하에서 스프린트 불가(바닐라 값 사용).

### 15.2 수면 (유저 동조)

```
유저가 침대에 누움 감지 → 주변 침대 탐색
  빈 침대 있음 → 봇도 그 침대에 누움(startSleeping)
  침대 없음    → 침대 옆 바닥에 눕는 포즈 연출(렌더 커스터마이즈)
유저 기상 → 봇도 기상
```
순수 교감 연출, 리스크 없음.

---

## 16. 기본 상태 (평상시 행동)

전투가 없을 때. (명령·교감은 현재 배제, 향후 염두 — 19장.)

```
평상시:
  유저 중심 8블록 반경에서 배회(어슬렁)

  유저가 12블록 밖으로 '달리면서' 나감:
    → 유저가 어디론가 향하는 것으로 간주, 따라옴
    → 유저 3블록 근처서 정지, 다시 배회

  유저 정지 → 주변 배회

  배고픈 상태(스프린트 불가):
    → 유저가 멀어져도 걸어서 따라옴(느려도)
    → 완전히 놓쳐도 걷기로 계속 추적(텔레포트 안 함)
```

---

## 17. 아이템·자원 조달

봇은 스스로 제작하지 않는다(유저 의존). 자원은 두 경로로만 얻는다.

### 17.1 유저가 던져준 것 수락

```
유저가 봇을 향해 아이템을 던짐:
  유저 조준선 오차범위 내(관대하게, 봇 중심 반경 넉넉히)로 떨어지면
    → "나에게 주는 것"으로 인식
  비전투 상황 → 달려가서 받음
  (전투 중엔 받으러 가지 않음)
```
너무 빡빡하게 조준을 요구하지 않는다 — 대략 봇 방향이면 수락.

### 17.2 유용한 드롭 대신 줍기 (제한적)

```
유저가 던진 것만 기본 수락.
자연 드롭(전리품)은:
  유저가 그것을 줍지 못하는 경우에만 봇이 주움
  (뺏어가는 느낌이 들지 않게)
```

---

## 18. 파라미터 총람

> 모든 값은 튜닝 대상. dev 클라이언트에서 관찰하며 조정한다.

### 체력 임계값

| 항목 | 값 |
|---|---|
| 봇 위급선 | 40% |
| 봇 이탈 위험선 | (튜닝, 예: 30%) |
| 봇 복귀선 | (튜닝, 예: 80%) |
| 봇 전투 중 회복 경계선 | (튜닝, 예: 55%) |
| 유저 타겟 전환선 | 40% |
| 유저 치명선(회복/납치) | 15% |

### 거리·범위

| 항목 | 값 |
|---|---|
| 근접 몹 유저 침범 반경 | 유저 중심 10블록 |
| 기본상태 따라오기 트리거 | 유저 12블록 밖(달리며) |
| 따라온 뒤 정지 거리 | 유저 3블록 |
| 배회 반경 | 유저 중심 8블록 |
| 워든 카이팅 밴드 | 16~20블록 |
| 물 낙법 사거리 | 약 4블록 |
| 던진 아이템 수락 반경 | 넉넉히(튜닝) |
| 도주 발동 위협 근접선 | 12블록 (구현 도출, T5.6 튜닝 대상) |
| 도주 해제 안전권선 | 18블록 (구현 도출, T5.6 튜닝 대상) |
| 낙하 받기 수평 판정 반경 | 2.5블록 (구현 도출, T5.6 튜닝 대상) |

> **구현 도출 파라미터 주기 (T4.5, T5.6 밸런싱 입력)**
> 설계 원문에 수치가 없어 구현 단계에서 정한 값. T5.6에서 실전 튜닝한다.
> - **도주 발동 위협 근접선 = 12블록**: 10.1은 발동을 「유저≤15%」로만 규정하나, 10.3-4/5의
>   「안전 방향 전력 질주 → 안전권 도달 시 stopRiding」이 성립하려면 "벗어날 위협"이 있어야 한다.
>   위협이 없는데 발동하면 하차 즉시 재발동해 mount↔dismount가 진동한다(실측 62회).
> - **도주 해제 안전권선 = 18블록**: 10.3-5 「안전권 도달 시 stopRiding」의 수치화.
>   발동선(12)보다 크게 잡아 히스테리시스를 만들어 진동을 차단한다.
> - **낙하 받기 수평 판정 반경 = 2.5블록**: 13.1 「봇이 유저 하강 경로 아래(수평거리 가깝고)」의 수치화.

### 전투 상수

| 항목 | 값 |
|---|---|
| 안전 낙하 높이(A* 기준) | 3블록 |
| 스프린트 불가 포만도 | 6 이하 |
| 활 풀차지 | 약 20틱 |
| 투사체 예측 반복 | 2~3회 |
| 화살 중력 / 항력 | ≈0.05 / ≈0.99 per tick (실측 확인) |
| 투척물 중력 | ≈0.03 per tick (실측 확인) |
| 엔더펄 자해 데미지 | 5(2.5칸) |
| 봇 스프린트 실측 속도 | 0.2806 blocks/tick (실측, T4.6) |
| 카이팅 진입 임계 (관측속도 ÷ 봇 스프린트) | 0.85 (구현 도출, T5.6 튜닝 대상) |
| 카이팅 해제 임계 (관측속도 ÷ 봇 스프린트) | 0.95 (구현 도출, T5.6 튜닝 대상) |
| 속도 관측 창 | 40틱 (구현 도출, T5.6 튜닝 대상) |
| 속도 관측 최소 표본 구간 | 20틱 (구현 도출, T5.6 튜닝 대상) |
| 카이팅 판정 유효 거리(접근 중 판정) | 대상과의 수평거리 > 4블록 (구현 도출, T5.6 튜닝 대상) |

> **[정정 · Phase 4→5 전환] 위 카이팅 임계·관측 창 4행은 아래 값으로 대체됐다** (기존 행은 이력 보존용으로 남긴다).
>
> | 항목 | 이전 | 확정 | 근거 |
> |---|---|---|---|
> | 속도 관측 창 | 40틱 | **60틱** | 수렴 기준(아래) |
> | 최소 표본 구간 | 20틱 | **60틱** | 창 전체가 차야 노이즈 보장 |
> | 카이팅 진입 임계 | 0.85 | **0.97** | 임계 1.0 대칭, 반폭 = 노이즈 3σ 여유 |
> | 카이팅 해제 임계 | 0.95 | **1.03** | 상동 |
>
> **창 선택 기준(결과 무관, 사전 선언)**: "측정 노이즈가 규칙1의 비교를 뒤집을 수 없는 최단 창"
> = 추정치 `mean ± 3σ`가 봇 스프린트(0.2806 b/t)의 한쪽에 머무는 최단 창. 듀티사이클이 다른 3종에서 확인:
>
> | 몹(이동틱 비율) | W20 | W40 | W60 | W100 |
> |---|---|---|---|---|
> | 좀비(1.00) mean±3σ | 0.114±0.004 ✓ | ±0.003 ✓ | ±0.002 ✓ | ±0.002 ✓ |
> | 워든(0.80) mean±3σ | 0.265±0.054 ✗ | ±0.027 ✗ | **0.265±0.004 ✓** | ±0.011 ✓ |
> | 브루트(0.58) mean±3σ | 0.066±0.104 ✓(상한 0.17) | ✓ | ✓ | ✓ |
>
> 임계 근처 대상은 워든뿐이며 W20·W40에서는 구간이 0.2806을 가로질러 판정이 노이즈로 뒤집힌다. **W60에서 처음 안정**된다.
>
> **임계 대칭화**: 설계 6.3 규칙1은 마진 없는 엄격 부등호(`적 속도 < 봇 스프린트`)다. 이전의 한쪽(1.0 미만) 밴드
> [0.85, 0.95]는 설계에 없는 안전 마진을 몰래 추가한 것이고, 그 폭이 워든의 실제 마진 5.4%
> (실측 0.2653 vs 0.2806)를 삼켜 설계와 반대 결론을 냈다. 밴드를 1.0 중심으로 옮기고 반폭은 노이즈에서 도출
> (W60에서 임계 근처 대상의 3σ ≈ 스프린트의 1.4% → 3% 채택).
>
> **신호 분리(채택)**: A = 대상 절대 변위÷경과틱(느리고 안정, 규칙1 입력) / B = d(gap)/dt(수 틱 반응,
> 규칙1 입력 아님 — 카이팅 실행 실패 감지·중단용). A가 느려도 위험하지 않은 이유가 B다.
>
> **무효 관측 게이트**: 봇 변위 < 0.02 b/t(갇힘) AND 대상 거리 < 3블록(밀착)이면 능력 추정을 갱신하지 않는다.
> 그 상황의 표본은 대상 능력을 말해주지 않으며, 무효 관측이 유리한 결론(카이팅 가능)을 만들어서는 안 된다.
| 몹 이동속도 어트리뷰트 → blocks/tick 환산 | ×0.157 (실측, T4.6) |
| 카이팅 밴드 여유 (하한/상한) | +1 / +5 (전 몹 공통, T4.6) |

> **구현 도출 파라미터 주기 (T4.6, T5.6 밸런싱 입력)**
> - **봇 스프린트 실측 속도 = 0.2806 b/t**: bot_warden_probe에서 직선 스프린트를 실측(= 바닐라 5.61 m/s).
>   규칙1을 "어트리뷰트 대 b/t"로 비교하던 단위 불일치를 없애기 위해 실측값으로 확정.
> - **몹 어트리뷰트 → b/t 환산 = ×0.157**: 같은 probe에서 워든(어트리뷰트 0.30)의 실이동을 0.0471 b/t로
>   실측해 역산. 이 환산 없이는 워든이 "스프린트 플레이어와 동급 속도"로 계산돼 6.5의 서술과 어긋났다.
> - **카이팅 밴드 여유 = 하한 +1 / 상한 +5 (전 몹 공통)**: 규칙3의 안전거리에 더하는 공통 상수.
>   워든의 층2 수평 사거리 15에 적용하면 문서값 16~20이 그대로 나온다. 몹별로 다른 여유를 쓰지 않으므로
>   "같은 규칙 엔진이 자동 도출"이 유지된다. (T3.2의 단일 여유 2.0은 bandDistance 용도로 그대로 둔다.)

> **[정정 · T4.6 사후 감사] 위 "몹 어트리뷰트 → b/t 환산 ×0.157"은 무효다.**
> 근거였던 워든 1회 관측(0.0471 b/t)은 워든이 EMERGE/ROAR 애니메이션에 묶이고 anger가 CALM이라
> 추격 상태가 아니었을 때의 값이었다. 평지 직선 추격으로 5종을 재측정한 결과(bot_speed_probe):
>
> | 몹 | 어트리뷰트 | 실측 평균 b/t | 실측 최고 b/t | 최고/어트리뷰트 |
> |---|---|---|---|---|
> | 좀비 | 0.23 | 0.1142 | 0.1142 | 0.496 |
> | 거미 | 0.30 | 0.1904 | 0.1943 | 0.648 |
> | 철골렘 | 0.25 | 0.1309 | 0.1349 | 0.540 |
> | 스켈레톤 | 0.25 | 0.1256 | 0.1349 | 0.540 |
> | 워든 | 0.30 | 0.1978 | **0.2798** | 0.933 |
>
> 비율이 0.496~0.933(1.88배 산포)이라 **단일 환산 계수 모델 자체가 부정확**하다. 또한 워든 최고속
> 0.2798 b/t는 봇 스프린트 0.2806 b/t와 **0.29% 차이**로 사실상 동급이다.
> 계수의 재확정과 규칙1 입력 모델(어트리뷰트 환산 → 관측 실측)의 변경은 설계 6.5 서술과 직결되므로
> 여기서 임의로 정하지 않는다. (이 블록은 측정 사실의 기록이며, 기존 등록 줄은 이력 보존을 위해 남긴다.)

### 워든 데이터 (층 2)

| 항목 | 값 |
|---|---|
| 체력 | 500 |
| 근접 최대(하드) | 45 |
| 소닉붐 고정 데미지 | 10(관통) |
| 소닉붐 사거리 | 수평 15, 수직 20블록 |
| 소닉붐 차징 | 1.7초(정지) + 쿨다운 1.3초 |
| 소닉붐 조건 | 감지 10초 + 마지막 공격 5초 경과 |

> **[정정 2 · Phase 4→5 전환 최종 감사] 창 채택 근거 재측정 · 밴드 반폭 재도출 · 규칙1 입력량 확정 · 통계 2단계**
> (추가만 했다. 위 블록들의 기존 문장은 한 줄도 수정·삭제하지 않았다.)
>
> **(A) 규칙1이 요구하는 양 = 후행 창 지속 추격속도.** 전 구간 평균이 아니다.
> bot_warden_live_speed에서 같은 시행의 두 값이 25% 어긋난 원인은 관측기 오차가 아니라 **구간 선택**이었다.
> 같은 시행에서 독립으로 잰 후행 60틱 창은 관측기 값과 최대 0.09% 차이다.
>
> | 시행 | 관측기 | 독립 후행60틱 | 독립 전구간 | 독립 경로길이 |
> |---|---|---|---|---|
> | t1 | 0.2658 | 0.2658 | 0.2118 | 0.2118 |
> | t2 | 0.2658 | 0.2658 | 0.2116 | 0.2117 |
> | t3 | 0.2236 | 0.2234 | 0.1963 | 0.2152 |
>
> 전구간 평균이 낮은 이유는 구간 앞부분의 경로탐색·가속 지체이며, 경로길이 속도가 변위 속도와 거의 같으므로
> 우회 경로 때문이 아니다. **채택 근거**: 카이팅은 지속 행위이므로 1회성 초기 지체가 섞인 전구간 평균을 쓰면
> 추격자를 체계적으로 과소평가하고, 그 오차는 항상 위험한 방향(카이팅 가능)으로 기운다.
>
> **(B) 확정된 양으로 재도출한 워든 마진 = 5.9%** (24.5%가 아니다. 24.5%는 폐기된 전구간 평균에서 나온 수다.)
> 창별 분산 재측정 기준 워든 W60 평균 0.2640 vs 봇 스프린트 0.2806 → 마진 5.92%.
> 실동 하니스 최악값 0.2658 기준 5.27%. 즉 **워든은 여전히 임계 근처이고, 나이프에지는 허상이 아니다.**
> W60 채택과 밴드 도출은 그대로 하중을 진다. 주의: bot_speed_probe의 워든 단발 최고 0.2798(마진 0.29%)은
> 규칙1의 입력이 아니다(피크 ≠ 지속).
>
> **(C) 창별 분산 재측정 — sd 비단조의 원인은 표본 부족이었다.**
> 이전 표는 셀당 독립 창이 ~2개뿐이었고, 중첩 창 추정치는 이웃끼리 표본을 공유해 산포를 과소평가한다.
> 표본을 2배로 늘리고(START_DIST 100, PER_MOB 900) 비중첩 창만의 sdIndep를 함께 재면 비단조가 사라진다.
>
> | 몹 | n | W20 | W40 | W60 | W100 | W150 |
> |---|---|---|---|---|---|---|
> | 좀비 n=783 | sd / sdIndep(nIndep) | .0018/.0018(39) | .0013/.0013(19) | .0010/.0011(13) | .0008/.0009(7) | .0006/.0007(5) |
> | 워든 n=302 | sd / sdIndep(nIndep) | .0184/.0188(15) | .0081/.0056(7) | **.0038/.0038(5)** | .0022/.0019(3) | .0018/—(2) |
> | 브루트 n=241 | sd / sdIndep(nIndep) | .0366/.0398(12) | .0242/.0314(6) | .0143/.0162(4) | .0151/—(2) | .0058/—(1) |
>
> **창 채택은 유지된다(W60).** 재측정값으로 수렴 기준을 다시 적용하면
> W40: 0.2639 + 3σ(0.0243) = 0.2882 > 0.2806 → 노이즈가 판정을 가로지른다(불안정).
> W60: 0.2640 + 3σ(0.0114) = 0.2754 < 0.2806 → 한쪽에 머문다(안정). W60이 여전히 최단 안정 창이다.
> 다만 근거의 강도가 바뀌었다: 이전의 "W60 sd 0.0013"은 독립 창 ~2개에 기댄 값이었고, 실제 값은 **0.0038**이다.
>
> **(D) 밴드 반폭 재도출: 3% → 4%.**
> W60 3σ = 3 × 0.0038 = 0.0114 = 봇 스프린트의 **4.06%**. 반폭 3%는 이 노이즈보다 좁아 도출 근거를 잃었다.
> 반폭의 허용 구간은 양쪽에서 잠긴다:
> - 하한: 반폭 ≥ 3σ(4.06%). 노이즈보다 좁은 밴드는 안정화 대상을 덮지 못한다.
> - 상한: 반폭 < 워든 마진(5.92%). 밴드가 유일한 임계 근처 대상을 삼키면 판정이 실측이 아니라 기억이 된다
>   (이전 [0.85,0.95] 밴드가 정확히 그 실패였다).
> 허용 구간 [4.06%, 5.92%)에서 **하한에 가장 가까운 4%**를 택한다. 마진까지 1.9%p 여유가 남는다.
> | 항목 | 이전 | 확정 | 근거 |
> |---|---|---|---|
> | 카이팅 진입 임계 | 0.97 | **0.96** | 반폭 = W60 3σ 4.06% → 4% |
> | 카이팅 해제 임계 | 1.03 | **1.04** | 상동 |
>
> **(E) 통계 판정 2단계.** 결정적 하니스(임계 1.00)는 유한 표본에서 Wilson 하한이 1.0에 닿을 수 없으므로
> "전 시행 통과"로 판정한다. 확률적 하니스는 두 단계로 나눈다.
>
> | 단계 | 시행 수 n | 통과 조건 |
> |---|---|---|
> | 개발 중 스크리닝 | 30 | Wilson 95% 하한 ≥ (스펙 임계 − 0.15) |
> | 확정 게이트 (T5.6) | 100 | Wilson 95% 하한 ≥ 스펙 임계 |
>
> 스크리닝 통과·확정 미통과는 결함이 아니라 **T5.6 확정 게이트 이월**이다. 스크리닝 미달은 **기능 결함**이다.
> 상수: `BotTestManager.SCREENING_TRIALS / SCREENING_SLACK / FINAL_TRIALS / SCREENING_MODE`.

---

## 19. 미결·향후 과제

### 19.1 설계 완성 전 결정 필요

없음 — 라이프사이클(3.3)까지 확정되어 본 설계는 닫혔다. 다음은 하단 개발 로드맵에 따른 시공.

### 19.2 향후 확장 (현재 배제, 염두)

2. **명령·교감 인터페이스** — 채팅/명령어/아이템 지시, 감정 표현. 현재 배제, 추후 설계.
3. **고급 이동** — 파쿠르(갭 점프), 본격 브리징. A* 이웃 규칙 확장으로 추가 가능.
4. **첫 교전 관측 학습** — 미지 몹 데이터를 싸우며 축적해 2회차부터 개선.

### 19.3 실측 검증 필요 (코드 단계)

| 항목 | 확인 내용 |
|---|---|
| 상주 배관 | 연결 없이 ServerPlayer를 틱 루프에 태우는 최소 뼈대(Carpet 참조) |
| A* 지형 조회 | 1.20.1 블록 통과/설치 가능/위험 판정 API |
| 강제 탑승 떨림 | 유저 격렬 이동 시 미세 떨림 정도 |
| 커스텀 팔 든 포즈 | 실제 렌더 모양 |
| 워든 밴드 유지율 | 딥다크 좁은 지형에서 밴드 깨짐 빈도 → 펄 의존도 |
| 투사체 상수 | 화살·투척물 중력/항력 실제 값 |

---

## 부록 A. 구현 상세

> 클래스·메서드 수준 스케치. 실제 시그니처는 셋업 후 `genSources` 디컴파일 소스로 확정한다.

### A.1 프로젝트 셋업

- **JDK 17** 고정(1.20.1). 21 사용 시 빌드 실패.
- **Forge 1.20.1 MDK** (47.x 최신), ForgeGradle.
- **Parchment 매핑** 추가 → 파라미터 이름 복원(Claude Code 정확도 향상).
- **`./gradlew genSources`** 필수 → 디컴파일 바닐라 소스를 프로젝트에 두면 `ServerPlayer`·`PathNavigation`·`LivingEntity.travel()` 등 실제 클래스를 직접 참조. 매핑 불일치 오류 최소화.

### A.2 핵심 클래스 스케치

```java
// 봇 본체
class AICompanionBot extends ServerPlayer {
    // 상속: 인벤토리, 배고픔, 아이템 사용, 전투 판정, 렌더
    Perception perception;
    ReflexLayer reflex;
    DecisionEngine decision;
    ActionExecutor action;

    @Override public void tick() {
        perception.gather(this);
        if (reflex.tryReflex(this)) { /* R0/R1/R2 처리됨 */ }
        else {
            Mode m = decision.select(perception);
            action.execute(this, m);
        }
        super.tick(); // 바닐라 플레이어 틱(배고픔·회복·아이템 사용 진행)
    }
}
```

```java
// 인식
class Perception {
    float botHealth, userHealth;
    int hunger, foodSaturation;
    List<TargetInfo> targets;      // 각 몹의 측정 스탯
    List<Projectile> incoming;     // 날아오는 투사체
    float fallDistance;            // 낙하 상태
    float expectedDamage;          // 예상 피해량(토템·이탈 트리거)
    boolean userFalling, userInBed;
}

// 몹 측정 정보
class TargetInfo {
    double maxHealth, attackDamage, moveSpeed, knockbackResist;
    boolean isRanged, armorPiercing; // armorPiercing은 층 2 보완
    double reach, rangedRange;
    boolean targetingUser;
}
```

```java
// 전투 규칙 엔진 (측정 → 전술)
class CombatRules {
    boolean canKite(TargetInfo t, double botSprintSpeed) {
        return t.moveSpeed < botSprintSpeed;
    }
    boolean allowMelee(TargetInfo t, CombatStats me, double safetyFactor) {
        double timeToKill = t.effectiveHp() / me.dps();
        double timeToDie  = me.effectiveHp() / t.dps(); // 관통 반영
        return timeToKill < timeToDie * safetyFactor;
    }
    double safeDistance(TargetInfo t, double margin) {
        return Math.max(t.reach, t.rangedRange) + margin;
    }
    boolean useShield(TargetInfo t) { return !t.armorPiercing; }
}
```

```java
// 자체 A* (능력 인식)
class BotPathfinder {
    Path findPath(BlockPos start, BlockPos goal, Capabilities cap);
    // 이웃 판정에서 cap.hasWaterBucket / hasBoat / placeableBlocks 참조
    // 경로 노드에 ActionTag(WATER_MLG / BOAT / PLACE_BLOCK) 부착
}

class Capabilities {
    boolean hasWaterBucket, hasBoat;
    int placeableCommonBlocks;
    int maxSafeFall = 3;
}
```

```java
// 시선 (자체 LookControl)
class LookController {
    void lookAt(Entity bot, Vec3 target, float maxYawStep, float maxPitchStep);
    // 몸통 yaw = 이동 방향, 머리 yaw/pitch = 주시 대상, 각속도 상한 보간
}
```

```java
// 투사체 예측 조준 (활·엔더펄 공용)
class ProjectileAiming {
    Vec3 predictAimPoint(TargetInfo t, double projGravity, double projDrag);
    float solveLaunchPitch(Vec3 aimPoint, double projGravity, double projDrag);
    // 반복 수렴 2~3회
}
```

### A.3 실행 매핑

| 판단 결과(Mode) | 실행(Action) | 상속/자체 |
|---|---|---|
| ENGAGE_MELEE | 크리 타이밍 루프 + `attack()` | 자체 로직 + 상속 메서드 |
| ENGAGE_RANGED | 예측 조준 + `startUsingItem(활)` | 자체 + 상속 |
| PEARL_ESCAPE/RETURN | 착지점 계산 + 엔더펄 투척 | 자체 + 상속 |
| RETREAT_HEAL | 전술 이동 후퇴 + 회복 아이템 사용 | 자체 + 상속 |
| 도주 모드 | A* 접근 + `startRiding` + 질주 | 자체 + 상속 |
| 배회/추종 | A* or 전술 이동 | 자체 |

### A.4 주의 사항 (구현 시)

- **크리는 스프린트 해제 + 하강 프레임**에서만. 스프린트 공격은 크리 무효.
- **공격 쿨다운 충전율 ≈1.0** 게이트. 연타 금지.
- **엔더펄 자해 5데미지** 사전 검사.
- **방패는 도끼에 5초 디스에이블**. 워든전에선 방패 무의미(끔).
- **오프핸드 우선순위**: 토템 > 방패.
- **A* 성능 예산** 필수(무한 탐색 렉 방지).
- **탑승은 SetPassengers 패킷 통지**(플래그만으론 디싱크).
- **소비 아이템 사용 중 이동 느려짐** — 황금사과·포션은 거리 확보 후.

---

# 개발 로드맵 (시공 순서)

> **이 문서의 위쪽(1~부록A)은 "무엇을 지을지"의 도면이고, 여기부터는 "어떻게 지을지"의 시공 순서다.**

## R.0 이 로드맵의 사용 규칙 (반드시 준수)

이 로드맵은 AI 코딩 에이전트가 태스크 단위로 실행하는 것을 전제로 작성되었다. AI는 **개별 태스크를 실행할 때 위쪽 설계 문서를 능동적으로 참조하지 못하고, 그 태스크에 적힌 문구대로만 구현하는 경향**이 있다. 따라서 다음 규칙을 지킨다.

### R.0.1 태스크 자기완결성 원칙

**각 태스크는 그 안에 구현에 필요한 모든 스펙을 다시 적는다.** "설계서 6장 참조", "위 내용대로" 같은 지시는 금지한다. 참조가 필요하면 그 내용을 태스크 본문에 복제해 넣는다. 태스크만 읽고도 100% 구현이 가능해야 한다.

### R.0.2 태스크 분할의 근거 원칙

태스크는 "쉬운 것부터"가 아니라 **의존성 근거**로 나눈다. 각 태스크에는 다음을 명시한다.
- **선행 조건(Depends on)**: 이 태스크가 시작되려면 무엇이 완성돼 있어야 하는가.
- **분할 근거(Why here)**: 왜 이 시점에 이 태스크가 오는가(어떤 기능이 있어야 이게 성숙하고, 이게 있어야 다음이 쌓이는가).

한 태스크에 너무 많은 기능을 몰면 오류가 여러 곳에서 동시에 터져 원인 격리가 불가능하다. 그렇다고 의미 없이 잘게 쪼개면 통합 비용만 는다. **"독립적으로 검증 가능한 최소 단위"**가 분할 기준이다.

### R.0.3 검증 명령어 원칙 (핵심)

**모든 태스크는 코드와 함께 검증 명령어를 만든다.** AI는 서버를 구동하고 로그를 읽을 수 있지만 **게임 화면은 보지 못한다.** 따라서 각 태스크의 완료는 다음 3단계로 이루어진다.

```
1. 기능 코딩
2. 검증 명령어 함께 코딩
3. AI가 서버를 열고 검증 명령어를 실행해 판정
```

검증 명령어의 필수 요건:
- **환경만 조성하고 대기한다.** 원하는 상황이 발생할 수 있는 조건(엔티티 스폰, 위치 배치, 아이템 지급 등)을 만들어 두고 관찰한다.
- **결과 변동으로만 판정한다.** "명령어가 호출됨 = OK"가 아니라, **어떤 값이 변했는지·조건이 바뀌었는지**를 측정해 성공/실패를 가른다. 예: 봇 y좌표가 감소했다(이동 성공), 타겟 체력이 줄었다(공격 명중), 봇 낙하 데미지가 0이었다(물낙법 성공).
- **로그에 명시적 PASS/FAIL과 측정값을 출력**한다. AI가 로그만으로 판정할 수 있어야 한다.

각 태스크 아래 `[검증]` 항목에 이 명령어의 사양을 적는다.

---

## R.1 로드맵 전체 구조

시공은 **6개 페이즈**로 나뉜다. 페이즈는 "이게 없으면 다음이 성립 불가"라는 순서로 배열된다.

```
Phase 0  프로젝트 토대        — 빌드·소스·검증 하네스가 없으면 아무것도 검증 불가
Phase 1  봇의 존재            — 봇이 월드에 살아있지 않으면 어떤 행동도 얹을 수 없음
Phase 2  이동과 시선          — 몸을 움직이고 봐야 전투·행동의 손발이 생김
Phase 3  전투 코어            — 측정·판단·기본 교전. 봇의 본체 능력
Phase 4  생존·반사·보호       — 코어 위에 얹는 생존/유저보호/특수반응
Phase 5  생활·자원·통합       — 평상시 행동, 자원 조달, 전체 결합과 밸런싱
```

각 페이즈 안에서 태스크는 T번호로 매긴다. 아래는 각 태스크의 **목적 / 선행조건 / 분할근거 / 구현 스펙(자기완결) / 검증**이다.

---

## Phase 0 — 프로젝트 토대

> **페이즈 근거**: 검증 하네스가 없으면 이후 모든 태스크의 3단계(코딩→검증코딩→검증)가 불가능하다. 가장 먼저 "검증할 수 있는 상태"를 만든다.

### T0.1 개발 환경 셋업

- **목적**: 빌드·실행 가능한 Forge 1.20.1 모드 스켈레톤 확보.
- **선행조건**: 없음.
- **분할근거**: 모든 것의 최하부. 이게 안 되면 컴파일조차 불가.
- **구현 스펙**:
  - JDK 17 고정(21 사용 시 빌드 실패).
  - Forge 1.20.1 MDK(47.x 최신) + ForgeGradle.
  - Parchment 매핑 추가(파라미터 이름 복원).
  - `./gradlew genSources` 실행 → 디컴파일된 바닐라 소스를 프로젝트에서 참조 가능하게. 경로를 기록해 이후 태스크가 실제 클래스 시그니처를 참조하도록.
  - 빈 모드가 로드되는지 확인(모드 등록, 로그에 모드 ID 출력).
- **[검증]** `./gradlew build` 성공 + dev 서버 기동 시 로그에 모드 로드 라인 존재. **판정: 빌드 exit code 0 AND 로그에 모드ID 문자열 존재.**

### T0.2 검증 하네스 (테스트 명령어 프레임워크)

- **목적**: 이후 모든 태스크가 쓸 "검증 명령어" 공통 골격 구축.
- **선행조건**: T0.1.
- **분할근거**: R.0.3의 검증 원칙을 실현하는 기반. 개별 검증 명령어들이 이 골격 위에 올라간다. 먼저 세워야 이후 태스크가 검증을 "함께 코딩"할 수 있다.
- **구현 스펙**:
  - 커맨드 등록 시스템(예: `/bottest <name>`).
  - 각 테스트는 다음 인터페이스: `setup()`(환경 조성) → `tick()`(관찰) → `judge()`(결과 변동 측정 → PASS/FAIL + 측정값 로그).
  - 타임아웃(N틱 내 조건 미충족 시 FAIL).
  - 결과를 로그에 `[BOTTEST] <name> PASS/FAIL measured=<값>` 형식으로 출력.
  - 깨끗한 테스트 환경 유틸: 슈퍼플랫/지정 좌표에 엔티티 스폰, 아이템 지급, 영역 청소.
- **[검증]** 더미 테스트("2초 후 무조건 PASS, 측정값 42 출력")를 실행해 로그에 `[BOTTEST] dummy PASS measured=42`가 뜨는지. **판정: 로그에 해당 형식 라인 존재.**

---

## Phase 1 — 봇의 존재

> **페이즈 근거**: 봇이 연결 없이 월드에 상주하며 매 틱 살아있지 않으면, 이동·전투·행동 어떤 것도 얹을 대상이 없다. A안(가짜 플레이어)의 유일한 핵심 관문이므로 여기서 확실히 넘는다.

### T1.1 상주 가짜 플레이어 (틱 배관)

- **목적**: `ServerPlayer`를 상속한 봇이 네트워크 연결 없이 월드에 존재하며 매 틱 `tick()`이 돌게 한다.
- **선행조건**: T0.2.
- **분할근거**: 봇 존재의 심장. 이게 되면 상속으로 인벤토리·배고픔·전투판정·렌더가 딸려온다. 안 되면 A안 전체가 무너지므로 최우선 검증.
- **구현 스펙**:
  - `class AICompanionBot extends ServerPlayer` 정의.
  - 연결 없이 틱 루프에 태우기: 더미 `Connection` 부착 방식 또는 서버 틱 이벤트에서 `bot.tick()` 직접 호출 방식 중 하나. (genSources로 1.20.1 `ServerPlayer` 생성자·`Connection` 요구사항 확인 후 결정.)
  - 클라이언트가 이 엔티티를 플레이어로 렌더하도록 스폰 패킷 + 플레이어 정보(탭 리스트 엔트리) 전송. 싱글플레이 통합 서버 기준.
  - Alex(마른 팔) 모델 스킨 적용.
  - 임시 소환 수단(개발용 명령어)로 봇 하나 스폰.
  - 봇은 매 틱 `super.tick()` 호출로 바닐라 플레이어 틱(배고픔·회복 등) 진행.
- **[검증]** 봇 스폰 후 N틱 동안 봇의 `tickCount`(또는 age)가 증가하는지 측정. 배고픔 값이 시간에 따라 바닐라대로 변하는지 관찰. **판정: 스폰 직후 tickCount와 N틱 후 tickCount 비교해 증가 AND 봇 엔티티가 월드에 존재(null 아님).**

### T1.2 라이프사이클 (스폰 에그·저장·사망·단일)

- **목적**: 3.3 라이프사이클 전체 구현.
- **선행조건**: T1.1.
- **분할근거**: 봇이 존재할 수 있게 된 직후, 그 존재의 생성·영속·소멸 규칙을 확정해야 이후 태스크가 "봇이 이미 있다"를 전제할 수 있다.
- **구현 스펙 (자기완결)**:
  - **스폰 에그(소모형)**: 커스텀 아이템. 우클릭 사용 시 봇 소환 + 에그 1개 소비.
  - **단일 제약**: 소환 시 이미 봇이 존재하면 소환 거부 + 채팅 메시지. 판단 근거는 아래 "존재 상태".
  - **존재 상태**: 월드 저장 데이터(SavedData)에 `botExists: bool` + `botUUID`를 기록. 소환/사망/복원의 단일 근거.
  - **영속 저장**: 봇의 위치·인벤토리·장착 장비를 UUID 기반으로 저장(ServerPlayer 데이터 저장 활용).
  - **복원**: 월드 로드 시 `botExists == true`이면 저장 데이터로 봇을 다시 스폰·상주(T1.1 배관 재실행). `false`면 스폰 안 함.
  - **사망**: 봇 사망 시 인벤토리 전체를 죽은 자리에 아이템 엔티티로 드롭 → 봇 엔티티 제거 → `botExists = false`로 해제(새 에그로 재소환 가능). 에그는 이미 소비됨.
- **[검증]** 세 개의 서브 테스트:
  1. 에그 2회 사용 시도 → 2번째 거부되고 봇 수는 1. **판정: 월드 내 봇 엔티티 카운트 == 1.**
  2. 봇 인벤토리에 표식 아이템 넣고 봇 킬 → 그 아이템이 드롭 엔티티로 바닥에 존재 AND `botExists == false`. **판정: 해당 아이템 엔티티 존재 AND 상태값 false.**
  3. 봇 소환 후 월드 리로드(저장→로드) → 봇이 같은 UUID로 다시 존재. **판정: 리로드 후 봇 엔티티 존재 AND UUID 일치.**

---

## Phase 2 — 이동과 시선

> **페이즈 근거**: 봇이 존재해도 스스로 움직이고 볼 수 없으면 전투·행동의 손발이 없다. 이동·시선은 이후 거의 모든 행동(교전·추종·회피·도주)이 호출하는 하부 실행기다.

### T2.1 이동 실행기 (입력 → travel 물리)

- **목적**: 봇이 좌표 텔레포트가 아니라 입력값 세팅으로 바닐라 물리를 통해 걷게 한다.
- **선행조건**: T1.1.
- **분할근거**: 모든 이동의 최하부 실행 계층. A*·전술이동·추종이 전부 이 위에 얹힌다. 길찾기보다 먼저 "입력하면 물리로 움직인다"를 확립.
- **구현 스펙**:
  - 매 틱: 몸통 yaw = 목표 방향, 전진 입력(zza)·좌우 입력(xxa) 세팅, 앞 블록 한 칸 높으면 점프 입력, 필요 시 웅크림(setShiftKeyDown).
  - 실제 이동은 바닐라 `travel()`이 처리(중력·충돌·계단·마찰). 좌표 직접 세팅 금지.
  - 개발용: "봇, 좌표 (x,z)를 향해 직진" 명령.
- **[검증]** 평지에서 봇에게 목표 방향 직진 지시 → N틱 후 봇 위치가 목표 방향으로 이동했는지. 한 칸 블록 앞에 두고 지시 → 봇이 점프해 올라섰는지(y 증가). **판정: 목표 방향 수평거리 감소 AND 계단 케이스에서 y 증가.**

### T2.2 시선 시스템 (몸통/머리 분리 + 각속도 보간)

- **목적**: 자연스러운 시선. 스냅이 아닌 부드러운 회전.
- **선행조건**: T1.1.
- **분할근거**: 이동과 독립적이나 같은 실행 계층. 전투 조준·주시가 이걸 호출하므로 전투 전에 완성. T2.1과 병렬 가능하나 둘 다 Phase 2 내.
- **구현 스펙**:
  - 몸통 yaw(yBodyRot) = 이동 방향, 머리 yaw/pitch(yHeadRot, xRot) = 주시 대상(독립).
  - 각속도 상한 보간: 현재각→목표각 차이를 틱당 최대 N도로 클램프해 접근.
  - 자체 LookControl(몹 의존 없음).
  - 개발용: "봇, 좌표 P를 주시" 명령.
- **[검증]** 봇 옆에 표식 엔티티 스폰 → 주시 지시 → 봇 머리 yaw가 여러 틱에 걸쳐(즉시 아님) 표식 방향 각도로 수렴하는지. **판정: 각 틱 yaw 변화량 ≤ 상한 AND 최종 yaw가 목표각 ±오차 이내.**

### T2.3 자체 A* (기본 지형)

- **목적**: 플레이어 히트박스 기준 격자 A* 길찾기(능력 확장 전, 기본 지형만).
- **선행조건**: T2.1.
- **분할근거**: 이동 실행기(T2.1)가 있어야 A*가 뽑은 노드를 실제로 걷게 할 수 있다. 능력 인식(물통·보트·블록)은 그 시스템들이 아직 없으므로 여기선 기본 지형만. 확장은 T4에서.
- **구현 스펙**:
  - 노드=블록 좌표. 이웃=인접+오르내림.
  - 이웃 유효성(플레이어 기준): 히트박스(0.6×1.8) 들어갈 공간, 발밑 받침, 1블록 오르기 가능/2블록↑ 불가, 낙하 3블록 이하 통과/초과 위험비용, 용암·선인장·낭떠러지 금지.
  - 비용: 기본 이동 + 위험 가중 + 평지 선호.
  - 성능: 탐색 노드 상한(예산), 여러 틱 분할, 경로 캐싱, 목표 미변 시 재계산 스킵.
  - 산출된 경로를 T2.1 이동 실행기로 노드 추종.
  - 개발용: "봇, 좌표 G까지 경로 이동" 명령.
- **[검증]** 장애물(벽·구덩이) 있는 지형에서 시작→목표 지시 → 봇이 목표 블록에 도달하는지(막힘 우회 포함). 도달 불가 지형이면 FAIL 아닌 "도달불가 반환" 확인. **판정: 봇 최종 위치 == 목표 블록(±1) within 타임아웃. 별도 케이스: 벽으로 완전 봉쇄 시 경로 null 반환.**

---

## Phase 3 — 전투 코어

> **페이즈 근거**: 이동·시선이라는 손발이 갖춰졌으니 봇의 본체 능력인 전투를 얹는다. 인식→측정→판단→기본교전이 서면, Phase 4의 생존·보호·특수반응이 이 위에 쌓인다.

### T3.1 인식 계층 (사실 수집)

- **목적**: 판단에 필요한 모든 사실을 매 틱 수집하는 Perception 구축.
- **선행조건**: T1.1.
- **분할근거**: 전투의 모든 판단이 인식 데이터를 소비한다. 판단·전술보다 먼저 "무엇을 아는가"를 확정.
- **구현 스펙**:
  - 수집: 봇 체력, 유저 체력, 배고픔/포만도, 주변 타겟 목록과 각 측정 스탯(최대체력·공격력·이동속도·넉백저항·원거리여부·히트박스·유저타겟팅 여부), 날아오는 투사체 목록, 봇 fallDistance, 예상 피해량, 유저 낙하/수면 상태.
  - 측정은 attribute·엔티티 상태에서 읽음(이름 하드코딩 없음).
  - 개발용: "봇 인식 덤프" 명령(현재 Perception 값을 로그로).
- **[검증]** 봇 앞에 좀비 스폰 → 인식 덤프 → 타겟 목록에 좀비가 있고 그 공격력·체력 측정값이 바닐라 좀비 값과 일치하는지. 화살 발사 → incoming 투사체 목록에 잡히는지. **판정: 타겟 목록 길이 ≥1 AND 측정 공격력==바닐라값 AND 투사체 감지 시 목록 반영.**

### T3.2 측정 기반 전술 규칙 엔진

- **목적**: 측정값 → 4규칙(카이팅/승패게이트/안전거리/방패·넉백) → 전술 파라미터 도출.
- **선행조건**: T3.1.
- **분할근거**: 인식이 준 스탯을 전술로 변환하는 두뇌. 실제 교전 실행(T3.3~)이 이 출력을 소비하므로 먼저 도출부를 세운다. 이 단계는 "판정"만, 실행은 다음.
- **구현 스펙 (자기완결)**:
  - 규칙1 카이팅: `적속도 < 봇스프린트속도` → 카이팅 가능.
  - 규칙2 승패게이트: `봇DPS로 적유효체력 죽이는 시간 < 적DPS로 봇유효체력 죽이는 시간 × 안전계수` → 근접허용, 아니면 원거리강제. (적DPS는 관통 반영.)
  - 규칙3 안전거리: `max(적근접도달, 적원거리사거리) + 여유` = 카이팅 밴드.
  - 규칙4 방패·넉백: 관통 적 → 방패오프, 넉백면역 적 → 거리 벌림.
  - 출력: {카이팅여부, 근접/원거리, 밴드거리, 방패on/off}.
  - 개발용: "봇, 타겟 T에 대한 전술 판정 덤프" 명령.
- **[검증]** 좀비(약함)·철골렘(고체력·넉백면역) 각각 스폰 후 판정 덤프. 좀비→근접허용, 철골렘→원거리강제+방패 판정 확인. **판정: 좀비 케이스 allowMelee==true AND 철골렘 케이스 allowMelee==false.**

### T3.3 근접 교전 (크리 타이밍)

- **목적**: 검으로 크리티컬 근접 전투.
- **선행조건**: T2.1, T2.2, T3.2.
- **분할근거**: 이동(스트레이프·접근)·시선(타겟 주시)·전술판정(근접허용)이 모두 있어야 근접 교전이 성립. 원거리보다 먼저(더 기본적이고 조준 엔진 불필요).
- **구현 스펙 (자기완결)**:
  - 바닐라 크리 조건: 낙하 중 + 지면 아님 + 사다리/물 아님 + **스프린트 아님** → 1.5배. 공격 쿨다운 충전율 ≈1.0 필요.
  - 루프: ①스프린트로 사거리 접근 → ②사거리+쿨다운충전 시 스프린트 해제·짧게 점프·하강 프레임에서 `attack()`(크리) → ③타격 후 재접근·쿨다운 재충전 반복 → ④타격 사이 원형 스트레이프.
  - 쿨다운 무시 연타 금지(충전율 게이트).
  - 전술 이동은 A* 우회 없이 직접 벡터 입력(지연 0).
- **[검증]** 무적 더미(또는 고체력) 타겟 스폰 → 봇에게 검 지급 → 근접 교전 지시 → 타겟 체력 감소량과 크리 발생 여부(데미지 크기로 판별) 측정. **판정: 타겟 체력 감소 AND 최소 1회 이상 크리 데미지(기본 공격의 1.5배 근사) 관측.**

### T3.4 투사체 예측 조준 + 원거리 교전

- **목적**: 활로 움직이는 타겟을 고명중 타격(엔더펄과 공용 엔진).
- **선행조건**: T2.2, T3.2.
- **분할근거**: 조준은 시선(T2.2)과 전술판정(원거리강제/카이팅)을 소비. 근접 다음에 오는 이유는 탄도 계산이 근접보다 복잡하고, 엔더펄(T4 생존)이 이 엔진을 재사용하므로 먼저 완성.
- **구현 스펙 (자기완결)**:
  - 반복 수렴: ①최근 틱 위치변화로 타겟 속도추정 →②현위치까지 비행시간 t →③예측위치=현위치+속도×t →④예측위치 기준 t 재계산 2~3회 →⑤발사각(pitch) 역산.
  - 투사체 상수: 화살 중력≈0.05/틱, 항력≈0.99/틱(실측 확인). 
  - 활 풀차지(약 20틱) 후 발사(급하지 않으면). `startUsingItem`.
  - 카이팅: 밴드 거리 유지하며 후퇴+사격(전술 이동 직접 벡터).
- **[검증]** 일정 속도로 직선 이동하는 타겟 스폰 → 봇에 활+화살 지급 → 원거리 교전 지시 → 발사한 화살이 타겟에 명중(타겟 체력 감소)하는지 여러 회 측정. **판정: 발사 대비 명중률 ≥ 임계(예: 80%), 즉 타겟 체력 감소 이벤트 수 / 발사 수.**

---

## Phase 4 — 생존·반사·보호

> **페이즈 근거**: 전투 코어가 서면, 그 위에 "죽지 않기·유저 지키기·특수 상황 반응"을 얹는다. 이들은 전부 인식·이동·교전을 호출하는 상위 로직이라 코어 다음에 온다.

### T4.1 생존 상태머신 (이탈·회복·펄)

- **목적**: 봇 자신의 체력 관리(8장).
- **선행조건**: T3.3, T3.4.
- **분할근거**: 교전이 있어야 "위험하면 교전을 중단하고 이탈"이 의미를 가진다. 엔더펄 이탈은 T3.4 조준 엔진 재사용.
- **구현 스펙 (자기완결)**: 우선순위 상태머신 — 생존이탈(위험선 미만, 펄 보유→PEARL_ESCAPE/없음→RETREAT_HEAL, 펄 자해 5데미지 사전검사)→회복유지(복귀선 미만)→펄복귀(복귀선 이상)→전투중회복(경계선 미만, 스플래시=즉발최우선/황금사과·포션=거리확보 후). 봇 위급선=40%. 값은 파라미터 총람 참조하되 여기 명시: 위험선 30%, 복귀선 80%, 경계선 55%(초기값, 튜닝).
- **[검증]** 봇 체력을 위험선 아래로 강제 세팅 + 적 배치 → 봇이 교전 중단하고 후퇴/펄 이탈하는지(봇↔적 거리 증가). 회복아이템 지급 시 사용해 체력 증가하는지. **판정: 지시 후 봇-적 거리 증가 AND 회복아이템 보유 시 봇 체력 증가.**

### T4.2 반사 계층 (토템·방패·투사체 회피)

- **목적**: 즉시 반응 R0·R1(7장).
- **선행조건**: T3.1, T2.1.
- **분할근거**: 인식(투사체·예상피해)과 이동(사이드스텝)이 있어야 반사가 성립. 판단보다 먼저 실행되는 계층이므로 상태머신(T4.1) 직후 결합.
- **구현 스펙 (자기완결)**:
  - R0 토템: 예상피해 추정(근접예상+투사체예상+환경) ≥ 현재체력+여유 → 오프핸드에 토템 장착·유지(바닐라 자동발동). 발동 후 이탈 전환.
  - R1 방패: 적 공격모션/투사체가 봇 히트박스로 향함 → 방패 올림 or 수직 사이드스텝. 방패 공방사이클(타격 직후 쿨다운창에 딜). 도끼적은 방패 디스에이블(5초) 주의.
  - 투사체 회피: 블레이즈·스켈레톤·쇠뇌피글린/피민·위더 투사체 무빙 회피.
  - 오프핸드 우선순위: **토템 > 방패**.
- **[검증]** (a) 봇 체력 낮추고 강한 근접 적 접근 → 봇 오프핸드에 토템 장착되는지(슬롯 확인). (b) 스켈레톤이 봇에 화살 발사 → 봇이 사이드스텝해 회피(화살 명중 안 함, 봇 체력 불변)하는지. **판정: (a) 오프핸드 아이템==토템, (b) 화살 발사 후 봇 체력 불변.**

### T4.3 유저 보호 프로토콜 (개입·타겟팅·원거리대응)

- **목적**: 9장 전체.
- **선행조건**: T3.2, T3.3, T3.4, T4.1.
- **분할근거**: 전투 판정·근접·원거리·생존이 다 있어야 "유저 상태에 따라 무엇을 우선 칠지"를 조합할 수 있다. 유저 보호는 전투 코어의 최상위 조율.
- **구현 스펙 (자기완결)**:
  - 개입: 불필요한 선제공격 금지. 유저 피격 시 방어 / 유저에게 어그로 끌린 적 선제 / 유저 전투개시 시 동참.
  - 타겟팅: 기본 최고위험 우선. 유저>40%→최고위험. 유저≤40%+봇정상→유저 노리는 적. 유저≤40%+봇위급(≤40%)→고위험적(유저방어우선, 위험감수), 고위험 여럿이면 유저 노리는 고위험. 유저≤15%+회복물약→유저 즉각회복(투척), 회복 후에도 ≤15%→도주(T4.5). 유저≤15%→도주.
  - 원거리 몹(유저 타격 시): 활 보유→유저 곁에서 활 처치+접근 근접위협 우선. 활 없음→①유저 어그로 근접 없으면 즉각 추적 처치 ②멀어지면 복귀(투사체는 무빙회피) ③유저 어그로 안 끌린 원거리 무시.
  - 근접 몹: 유저 중심 10블록 침범 시 타격 대상.
- **[검증]** (a) 유저(더미 플레이어/스탠드) 옆에 좀비 어그로 → 봇이 선제 처치하는지. (b) 고위험·저위험 적 동시 배치, 유저 체력 정상 → 봇이 고위험부터 치는지(고위험 적 체력이 먼저 감소). **판정: (a) 좀비 체력 감소 시작, (b) 두 적 중 고위험 적 체력이 먼저 감소.**

### T4.4 환경 조작 (크리퍼 벽·엄폐·낙하생존)

- **목적**: 11·12장. 블록 설치 방어 + 봇 낙하 생존.
- **선행조건**: T3.1, T2.1, T4.2.
- **분할근거**: 인식(크리퍼 fuse·낙하상태)과 이동, 반사 계층이 있어야 설치·낙법이 반사로 작동. 크리퍼 특수대응은 여기서.
- **구현 스펙 (자기완결)**:
  - 구조물 배치기: 크리퍼 점화 감지→크리퍼와 유저/봇 사이 직선상 블록 설치(폭발 감쇠)+자기 방패/블록+점화거리 밖 스텝. fuse 부족 시 설치 대신 방패+이탈. 화살 엄폐·긴급 격벽 포함. 블록 선택: 고가치 제외, 흔한 것 우선.
  - 낙하 생존 R2: 낙하중(fallDistance>안전값) AND 아래거리≤사거리 → 물통 보유 시 아래에 물(데미지 가능성만 있어도, 최우선), 없고 치명적이면 슬라임/짚/침대 설치 or 보트 놓고 탑승.
- **[검증]** (a) 봇을 높은 곳에서 낙하 + 물통 지급 → 착지 시 낙하 데미지 0인지(봇 체력 불변). (b) 유저 옆에 크리퍼 점화 유도 + 봇에 블록 지급 → 크리퍼-유저 사이에 블록 설치되는지(해당 좌표 블록 변화). **판정: (a) 낙하 후 봇 체력 불변, (b) 지정 좌표가 air→고체블록 변화.**

### T4.5 도주 모드 + 유저 낙하 받기 (탑승)

- **목적**: 10·13장. 탑승 메커니즘.
- **선행조건**: T2.3, T4.3, T4.4.
- **분할근거**: 탑승은 A*(접근)·유저보호(도주 발동조건)·낙하감지(받기)를 모두 소비. 가장 복합적이라 생존/보호/환경이 선 뒤에 온다.
- **구현 스펙 (자기완결)**:
  - 도주 발동: 유저≤15%. 투척 회복포션 있으면 1순위 투척(회복되면 취소), 없거나 회복 후에도 ≤15%면 즉각 startRiding 후 질주.
  - 탑승: 서버에서 `player.startRiding(bot)` → SetPassengers 패킷 통지(플래그만 아님). passenger 오프셋 머리 위, 두 팔 든 포즈 렌더. 시점 자유·이동 봇 담당. 안전권서 stopRiding.
  - 유저 낙하 받기(능동): 유저 낙하중 + 봇이 하강경로 아래 → 탑승 받기(데미지0). 아니면 마중나가 받기, 실패 시 물/블록 폴백.
- **[검증]** (a) 유저(더미) 체력 15% 이하 + 회복포션 없음 + 적 위협 → 봇이 유저를 탑승시키는지(유저의 vehicle == 봇). (b) 유저를 봇 위로 낙하 → 봇이 받아 유저 낙하데미지 0. **판정: (a) 유저.getVehicle()==봇, (b) 받기 후 유저 체력 불변.**

### T4.6 특수 케이스 데이터 (워든)

- **목적**: 6.5 워든 층2 데이터 + 소닉 차징 반사.
- **선행조건**: T3.2, T3.4, T4.1, T4.2.
- **분할근거**: 일반 엔진(측정·조준·생존·반사)이 완비돼야 워든 데이터를 "규칙 입력"으로만 얹어 전술이 자동 도출된다. 전용 로직이 아니라 데이터 보완임을 이 순서가 보장.
- **구현 스펙 (자기완결)**:
  - 워든 데이터(측정 불가분): 소닉붐 관통=true, 소닉붐 사거리 수평15/수직20, 차징 신호(가슴 파티클/상태). 이 값을 규칙3(안전거리→밴드16~20)·규칙4(방패오프)에 입력.
  - 소닉 차징 감지 반사: 차징 시작 감지 시 즉시 밴드 밖 이탈(후퇴 or 펄).
  - 규칙1이 워든 저속을 읽어 카이팅 자동 판정, 규칙2가 근접금지 자동 판정.
- **[검증]** 워든 스폰(또는 워든 스탯 더미) → 봇 전술 판정 덤프가 {카이팅=true, 원거리강제, 밴드16~20, 방패off}인지. 소닉 차징 상태 유발 → 봇이 밴드 밖으로 이탈하는지(봇-워든 거리 증가). **판정: 전술 판정 4값 일치 AND 차징 시 봇-워든 거리 증가.**

---

## Phase 5 — 생활·자원·통합

> **페이즈 근거**: 전투·생존이 완성된 봇에 평상시 행동·자원 조달을 붙이고, 전체를 하나의 봇으로 결합해 밸런싱한다. 이건 모든 하위 시스템이 있어야 조율 가능하므로 마지막.

### T5.1 능력 인식 A* 확장 (물통·보트·파일러업)

- **목적**: 4.4. 기본 A*에 능력 기반 이웃 추가.
- **선행조건**: T2.3, T4.4, T4.5, T14(장비관리 재고).
- **분할근거**: 물낙법(T4.4)·보트낙법(T4.5)·블록 설치·블록 가치판정이 실제로 구현된 뒤에야 A*가 "이 능력을 쓰는 경로"에 액션 태그를 달 수 있다. 실행기 없이 태그만 달면 무의미하므로 이 순서.
- **구현 스펙 (자기완결)**: 능력 스냅샷(물통·보트·흔한블록수) → 하강 규칙(3칸초과+물통→MLG태그, 대형+보트→보트태그), 상승 규칙(막힘+블록→파일러업태그), 특수이동 비용 페널티. 액션 태그는 R2/구조물배치기가 실행.
- **[검증]** 3칸 초과 절벽 앞 + 물통 보유 → A* 경로가 절벽 아래로 내려가는 경로를 반환(우회 대신)하고 낙법 액션 태그 포함, 실제 이동 시 데미지 0. 블록만 있고 절벽이면 우회. **판정: 물통 보유 시 직하강 경로 채택 AND 하강 후 봇 체력 불변.**

### T5.2 장비 관리자

- **목적**: 14장.
- **선행조건**: T3.1.
- **분할근거**: 인식(인벤토리)만 있으면 독립 작동. 전투 태스크들이 "최적 무기 스왑"을 요청하므로 Phase 3~4와 병행 가능하나, 밸런싱 관점에서 통합 페이즈에 배치. (실제로는 T3 이후 언제든 삽입 가능 — 선행조건이 가벼움.)
- **구현 스펙 (자기완결)**: 갑옷 4슬롯 점수화(방어도+강도+인챈트−내구위험) 최고착용. 무기 근접/원거리 최고 선정 스왑(공격력+속도+인챈트, 언데드면 강타↑). 방어용 블록 재고 추적(고가치 제외). 인벤토리 변화 시 재평가.
- **[검증]** 봇에 철갑옷·다이아갑옷 동시 지급 → 봇이 다이아 착용(더 높은 방어도)하는지. 나무검·다이아검 지급 → 근접 시 다이아검 사용하는지. **판정: 착용 갑옷==다이아 AND 메인핸드==다이아검.**

### T5.3 생활 기능 (배고픔·수면)

- **목적**: 15장.
- **선행조건**: T3.1.
- **분할근거**: 인식만 있으면 독립. 전투와 무관한 백그라운드라 통합 페이즈에서 추가.
- **구현 스펙 (자기완결)**: 배고픔 임계 이하→최적 식량 섭취(위험식량 회피, 전투중 억제). 포만도 6 이하 스프린트 불가. 유저 침대 취침 감지→빈 침대 있으면 봇도 취침, 없으면 옆 바닥 눕는 포즈. 유저 기상 시 기상.
- **[검증]** 봇 배고픔 강제로 낮춤 + 식량 지급 → 봇이 먹어 배고픔 회복하는지. 유저 취침 + 옆 빈 침대 → 봇도 취침 상태 되는지. **판정: 식량 섭취 후 배고픔 값 증가 AND 유저 취침 시 봇 isSleeping==true.**

### T5.4 기본 상태 (배회·추종)

- **목적**: 16장.
- **선행조건**: T2.3, T5.3.
- **분할근거**: A*(추종 이동)와 배고픔(걷기 추종 조건)이 있어야 완성. 평상시 행동이라 전투 밖.
- **구현 스펙 (자기완결)**: 유저 중심 8블록 배회. 유저 12블록 밖으로 달리며 나감→따라옴, 유저 3블록 근처서 정지 후 배회. 유저 정지→배회. 배고픔(스프린트 불가) 상태로 유저 멀어짐→걸어서 따라옴, 완전히 놓쳐도 걷기 추적(텔레포트 안 함).
- **[검증]** 유저를 봇에서 12블록 밖으로 이동 → 봇이 따라와 3블록 근처서 정지하는지. **판정: 봇-유저 거리가 3블록 근처로 수렴 AND 정지(속도≈0).**

### T5.5 자원 조달 (던진 것 수락·드롭 줍기)

- **목적**: 17장.
- **선행조건**: T2.3, T3.1.
- **분할근거**: 인식(드롭 아이템)과 이동(달려가서 받기)이 필요. 평상시 상호작용.
- **구현 스펙 (자기완결)**: 유저가 봇 향해 던진 아이템(조준선 오차범위 내, 넉넉히) → "나에게 주는 것"으로 인식, 비전투 시 달려가 받음(전투 중 안 감). 자연 드롭은 유저가 못 줍는 경우에만 봇이 주움(뺏는 느낌 방지).
- **[검증]** 비전투 상태에서 유저가 봇 방향으로 아이템 던짐 → 봇이 다가가 인벤토리에 획득하는지. **판정: 던진 아이템이 봇 인벤토리에 추가됨(엔티티 소멸 + 봇 인벤 수량 증가).**

### T5.6 전체 통합·밸런싱

- **목적**: 모든 시스템을 하나의 봇 tick 루프에 결합하고 파라미터 튜닝.
- **선행조건**: T5.1~T5.5 및 그 이전 전부.
- **분할근거**: 모든 하위 시스템이 완성돼야 4계층(인식→반사→판단→실행)의 최종 우선순위 충돌을 조율하고 파라미터(18장 총람)를 실전 튜닝할 수 있다. 시공의 마지막.
- **구현 스펙 (자기완결)**: 매 틱 4계층 순서 실행. 계층 간 우선순위 충돌 해소(반사>판단, 생존>교전, 유저보호 조율). 18장 파라미터 총람 값 적용 후 시나리오별 튜닝(워든전 밴드유지율, 도주 트리거 적절성 등).
- **[검증]** 복합 시나리오: 유저+봇에 다수 몹(근접·원거리·크리퍼) 배치 → 봇이 크래시 없이 우선순위대로 대응(유저 방어·회피·생존 이탈)하며 N분 생존하는지. **판정: N분간 예외 로그 0 AND 봇·유저 생존 AND 각 시스템 발동 로그(교전·회피·회복) 관측.**

---

## R.2 검증 하네스 상세 (T0.2 확장 사양)

각 검증 명령어가 공유하는 판정 철학을 재확인한다.

- **호출≠성공**: 명령어가 실행됐다는 사실은 성공이 아니다. **측정 대상 값의 변동**만이 성공 근거다.
- **측정 예시**:
  - 이동: 시작 좌표 vs 종료 좌표의 거리 변화.
  - 공격: 타겟 체력 감소량(크리 여부는 감소 폭으로).
  - 회피: 공격 후 봇 체력 불변.
  - 낙법: 낙하 후 봇 체력 불변.
  - 상태: `botExists`, `getVehicle()`, `isSleeping()`, 오프핸드 슬롯 아이템 등 상태값 비교.
- **환경 조성 후 대기**: 테스트는 원하는 상황이 나올 수 있는 조건(스폰·배치·지급)만 만들고, 관찰 창(N틱) 동안 조건 변동을 폴링한다.
- **로그 출력**: `[BOTTEST] <name> PASS/FAIL measured=<before→after> expected=<조건>`. AI는 이 라인만으로 판정.

---

*문서 끝. 시공은 Phase 0(T0.1 셋업 → T0.2 검증 하네스)부터 시작하며, 각 태스크는 코딩→검증코딩→서버구동검증의 3단계를 거쳐 다음으로 넘어간다.*
