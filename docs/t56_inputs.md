# T5.6 입력 목록 (블록 R 마감 시점)

기준 커밋: 블록 R. 이 문서는 T5.6 지시의 골격이며, 각 항목은 **값과 출처**를 함께 싣는다.
값 없는 항목은 넣지 않는다.

---

## 1. 게이트 최종 상태

**먼저 분모를 밝힌다.** 「게이트 8개」의 정본 목록은 이 저장소에 없다 — 사용자 감독 문서의
번호 체계이고, 저장소에는 게이트 1·3·4·7·8만 지시문에서 이름이 나왔다. **게이트 2·5·6은
내가 가진 어떤 문서에도 정의가 없다.** 유형 #14의 규약대로, 없는 분모를 지어내지 않고
아는 것만 값으로 적는다.

| 게이트 | 상태 | 닫힌 근거 값 |
|---|---|---|
| 1(a) 미설명 비결정성 원인 | **닫힘** | 격리 결함을 잎 `distance`(이웃 파생 속성)로 특정하고 판정 코어를 시공 범위로 좁힌 뒤, creeper_wall·creeper_lowfuse·path_blocked 각 n=10 = 30 트라이얼에서 canary MISMATCH **0**. 뒤집힘 소멸 |
| 1(b) 본질적 확률성 분류 | **부분** | 지목 10종 n=10 전부 10/10 또는 0/10(흔들림 0). 값으로 확인된 [확률적]은 `bot_dodge`(18/50 두 번), `bot_coldstart_*`(이봉 분포), 그리고 **블록 R에서 새로 밝혀진 `bot_protect_intervene`(9/10, aggroDrop 0.0~97.4)**. 나머지는 [결정적] 잠정 |
| 3 · 4 | **닫힘** | M-3: 계측 49개 판정 라인 전부 `idleCommandedTicks:0`, `moveOwnerAnomalyTicks:0`. 그중 40개는 `idleOwnedTicks>0`(최대 574) — 분기 진입과 자율 이동이 다른 값임을 확인 |
| 7 scenarioSpec 분모 | **닫힘, 단 분모가 정정됨** | 58/58이 아니라 **96/96**(BotTestRegistry 등록 이름 기준). 58은 계측기가 만든 분모였다(유형 #14) |
| 8 §6 실측 상수 | **닫힘** | 봇 스프린트 0.2806 → 0.2806, 좀비 pk/attr 0.496 → 0.496, 워든 단발 최고 0.2798 → 0.2798, 워든 추격 0.2766±0.0068 → 30회 평균 0.2784(0.26σ), coldstart 0.1937/0.0883 → 0.1950/0.0902. 9종 전부 `idleCommandedTicks:0` |
| 2 · 5 · 6 | **정의 없음** | 저장소의 어떤 문서에도 항목 정의가 없다. 지어내지 않는다 |

---

## 2. T5.6이 입력으로 받을 항목

### 2.1 구현 슬롯 (기능 코드 변경 필요)

| # | 항목 | 근거 값 |
|---|---|---|
| I-1 | **규칙2를 근접 컨트롤러에 배선** | M-6 / 유형 #10 확정. `bot_rule2_deny`가 `allowMelee:false`인데 `moveOwner:MELEE:200`, `damageDealt:88.6` — 판정은 옳고 행동이 그것을 읽지 않는다 |
| I-2 | **D칸 발행 경로 신설** | M-7(1) 미구현 확정. `BotRangedCombat.java:186`이 유일한 횡이동 발행 지점이고 근접이 대상을 쥐면 도달 불가. `bot_rule_dcell` **0/10 결정적 FAIL**(비결정성 아님) |
| I-3 | **신호 B 술어 변경** | O-0. 설계서:959-961이 스스로 결함으로 명시(「B의 입력 d(gap)/dt는 「간격이 줄어드는가」를 묻는데, 잡아야 할 실패 상태는 「간격이 이미 0이고 벌어지지 않는가」다」). 확인 값: `execMonitorFired:false, ticksToExec:-1, gapAtFlee:0.91`, 대조군 `bot_kite_execmon_lat` PASS |
| I-4 | **회피 성능** | Q-2. 이월 목표는 하한 아닌 **필요 점추정 p̂ 0.840**, 현재 0.815, 격차 **+2.5pp = 400발 기준 +10발** |

### 2.2 측정 기반·기준선 (튜닝 입력)

| # | 항목 | 근거 값 |
|---|---|---|
| B-1 | **execmon 대가 기준선** | `failingRatio` **0.496 ± 0.015** (n=3, cv 3.0%). `hpPerIntentTick`은 cv 49%로 **쓸 수 없다** — B 수정 전후 비교에 쓰지 마라 |
| B-2 | **워든 마진 앵커 전제 불일치** | 설계서:866은 마진을 두 앵커로 적는다 — W60 평균 0.2640(5.92%), 실동 최악값 0.2658(5.27%). 두 번째 앵커의 생산 하니스가 O·Q에서 0.0000을 냈고 R에서 0.2597/0.2658/0.2658로 돌아왔다. **같은 코드가 월드마다 갈린다**(§3 참조) |
| B-3 | **좀비 창별 분산 새 기준선** | 설계서:881 좀비 행 W20~W150 `.0018/.0013/.0010/.0008/.0006` → `.0145/.0115/.0098/.0080/.0066`(8~11배). **결정 파급 0** — :796의 네 칸이 전부 ✓ 유지, 뒤집히려면 sd 0.0565(3.9배) 필요. 워든·브루트 행은 재현 |
| B-4 | **[확률적] 하니스 확정** | `bot_dodge`(18/50 재현), `bot_coldstart_dist`+tail1/2/3(이봉), `bot_protect_intervene`(9/10) |
| B-5 | **user-absent 검증 한계** | `bot_rule1_kite/nokite`, `bot_rule4_pierce/normal/axe` 5종은 `TestUser`를 띄우지 않아 `BotProtection.tick`이 `user == null`에서 즉시 반환한다. 규칙 판정은 봤고 **유저 보호 경로는 못 봤다** |
| B-6 | **월드 시드 미고정** | 러너가 하니스마다 `rm -rf run/world`를 하므로 실행마다 지형이 새로 생성된다. 「단일 변수 팔」이 실제로는 2변수였다(§3). **T5.6 기준선 실행 전에 시드 고정 여부를 정해야 한다** |
| B-7 | **판정 라인 인코딩** | 콘솔 appender는 US-ASCII로 떨어지고 파일 appender는 UTF-8이다. 게이트가 `run/logs/latest.log`를 읽도록 바꿨다. 1차 보완 T6.1의 「기준선 항목에 scenarioSpec 동봉」은 이 경로를 써야 한다 — 깨진 전제는 동봉해도 재구성 불가다 |
| B-8 | **T1.2 복원 검증 통로** | Q-4에서 `bot_persist_load` PASS(`restored:true, uuidMatch:true`). 1차 보완 **T6.3(파괴 델타 P1, ownerUUID 마이그레이션)**의 선행조건이 열렸다 |
| B-9 | **DIRTY 탐지기는 3중이 아니라 2중** | 지문 성분 3(src 인덱스)은 성분 2(워킹트리)와 항상 함께 발화하므로 **독립 탐지력 미증명**이다. 결함은 아니다 |

---

## 3. [BROKEN] — 지금 FAIL로 남기는 하니스

| 하니스 | 상태 | 왜 지금 고치지 않는가 |
|---|---|---|
| `bot_kite_execmon` | FAIL (n=3, 전 트라이얼 `execMonitorFired:false`) | **알려진 결함의 재현**이다. 설계서:959-961이 술어를 바꾸지 않은 이유까지 적어 두었고, 술어 변경은 B의 정의 변경이므로 T5.6에서 승인 하에 한다(I-3) |
| `bot_rule_dcell` | FAIL 0/10 (결정적) | **미구현**이다. 발행 경로가 없으므로 고치는 것은 수리가 아니라 신설이다(I-2) |
| `bot_protect_intervene` | FAIL 9/10, canary OK | **[INVALIDATES]** — 이전 PASS 3/3은 표본 부족이었다. 판정 임계와 트라이얼 수가 스펙에서 나와야 하므로 T5.6의 통계 정리 대상이다. 행동 결함인지 임계 문제인지 아직 갈리지 않았다 |
| `bot_window_variance` | PASS이지만 좀비 행 무효 | 하니스는 통과한다. **기록값이 무효**일 뿐이며 결정 파급이 0이므로(B-3) 재측정을 T5.6으로 미룬다 |
| `bot_warden_live_speed` | 현재 PASS (O·Q에서는 FAIL) | **사상자가 아니었다.** 같은 코드가 월드마다 갈린다 — 원인은 하니스가 아니라 시드 미고정(B-6)일 가능성이 높고, 그것을 먼저 정해야 이 하니스의 판정이 의미를 가진다 |

---

## 4. 부채 (T5.6 입력은 아니나 열려 있음)

- `bot_warden_live_speed`의 월드 의존성 — 시드를 고정한 상태에서 고정/해제 팔을 다시 갈라야 한다.
- `bot_kite_execmon` 계열의 07-25 기준값(hp 15.7)에 구간 길이 기록이 없어 정규화 비교 불가.
- `bot_warden_probe`의 `wardenChase:0.0477`이 §6의 어느 상수에 대응하는지 미기록.
- 판정 문자열에 한글을 쓰면 콘솔 경로에서 깨진다 — 파일 경로로 우회했으나 근본 해결은 아니다.
